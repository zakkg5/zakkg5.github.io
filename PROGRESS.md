# PROGRESS.md — 進度與決策紀錄

> 這份文件記錄「做到哪」和「為什麼這樣決定」。
> 規格看 SPEC.md,互動語彙看 motion-patterns.md,這份只記進度與待辦。
> 最後更新:2026-08-02

---

## 怎麼繼續

```bash
cd C:\Users\zakk\zakk-portfolio
python -m http.server 8765        # 啟動本機伺服器
```
然後瀏覽器開 `http://localhost:8765`

- 診斷面板:`http://localhost:8765/?debug`
- 素描去背對照:`http://localhost:8765/_dev/sketch-compare.html`

在這個資料夾執行 `claude` 並用 `/resume` 可以接回之前的對話。

---

## 已完成

### 31. 拆掉罐頭字彙:載入改比例尺、換頁改繪圖機掃描(2026-07-29)

Zakk 的回饋:「**AI 感還是太強**」。診斷後認定問題不在做得好不好,
而在**用的是罐頭字彙**,三個來源:

1. **載入動畫**是大數字倒數 + 細線 + `Loading` —— 幾乎每個作品集模板都有
2. **通用飄浮微塵**是 canvas 粒子教學的產物,把真正原創的龍骨碎片稀釋掉了
3. **首屏沒有字的主張**:最大的字 `Portfolio` 只有 30.8px,
   比下面 Works 清單的 44px 還小 → 個性全部由粒子承擔,而粒子最沒個性

**結論:網站上最不像 AI 的是內頁的「圖面直接印在紙上」。**
那是從 Zakk 自己的材料長出來的(1:100、比例尺、北方箭頭、材質圖例)。
方向是把那套語言往前推,換掉罐頭的部分 —— 整站只講一句話:
**「這裡的東西都是畫出來的。」**

**① 載入 → 建築比例尺**(Zakk 選)
- 拿掉大數字與 `Loading`。改成黑白相間的比例尺被「描」出來,
  刻度是圖上那組 `0 / 50 / 125 / 250 / 500 / 1000`,尾端橘線是**筆的位置**
- 放**左下角**(圖紙上比例尺本來待的地方),畫面中間讓給頭骨碎片
- SVG + `<clipPath>`,JS 只改 clip 的寬度與筆的 transform;進度仍綁真實載入
- `vector-effect: non-scaling-stroke` —— 不然非等比縮放會把 1px 的線拉粗
- ⚠️ 這推翻了 DECISIONS 原本「版面抄 Daima:大數字 + 細橘線」的決定

**② 拿掉通用微塵層**(Zakk 選)
- ambient canvas 原本有 420 顆全站飄浮微塵,已移除。現在只畫**區塊碎片雲**
- 首屏頭骨(另一張 canvas)不受影響
- ⚠️ **踩到的坑**:用 Python `str.replace()` 移除 `ctx.setTransform(...); build();`
  時,**兩個 IIFE 的 layout() 有一模一樣的程式碼**,replace 會全部換掉 ——
  連首屏頭骨的 `build()` 也被刪了,結果兩張 canvas 都變全白。
  查的時候 console 沒有任何錯誤,是用 `getImageData` 數有色像素才發現的

**③ 換頁 → 繪圖機掃過**
- 從交叉淡入淡出改成:新頁 `clip-path` 由左往右**被描出來**,蓋在舊頁上
- 舊頁**不動**(`vtHold`),就留在原地被蓋掉 —— 兩邊同時淡會糊成一團,
  而且「被覆蓋」本來就比「一起消失」更像換一張圖紙
- 跟內頁圖面的進場是同一個動作

**④ 到達新頁時不要再演一次進場(這是 Zakk 說的「切換頁出現的動畫」)**
- 第一次做錯了:把 `.reveal` 延後到轉場結束才開始 → 掃描描出來的是**一頁模糊的字**
  (元素停在進場前的狀態:透明 + blur 10px)
- 想通了:**掃描本身就是這一頁的進場**,同一件事不該演兩次。
  改成 `pagereveal` 時把首屏內的 `.reveal` 直接標成已顯示,
  並用 `html.vt-arrive` 關掉那一幀的 transition(不然會看到它們「跳」一下),
  隔兩幀再放開。首屏以下不動,照常捲到才進場
- 直接開網址(沒有轉場)完全不受影響 —— 實測 28 個 reveal,首屏 4 個正常浮現

**實測**:60 FPS、無 console 錯誤、無橫向捲動;
掃描中途截圖確認左側已描出的內容是**清晰成形**的,右側仍是舊頁。

---

## 69. 上線前檢查(2026-08-03,**目前狀態 —— 卡在網址還沒確認**)

Zakk:「可以上線了」。跑了一輪上線前檢查,**有一個真的會出事的問題**。

### 🔴 網址是佔位的,而且兩個帳號名對不起來

| 位置 | 現在寫的 |
|---|---|
| `index.html` canonical / og:url / og:image | `https://zakktseng.github.io/` |
| `sitemap.xml`(9 筆) | `https://zakktseng.github.io/...` |
| `robots.txt` Sitemap | `https://zakktseng.github.io/sitemap.xml` |
| **About 區的 GitHub 連結** | **`https://github.com/zakkg5`** |

`index.html` 的註解本來就寫著「上線前要換成實際網址」——**它是佔位的**。
如果 GitHub 帳號是 `zakkg5`,Pages 的網址就是 `https://zakkg5.github.io/`,
上面五個地方全錯:canonical 指到不存在的網域、分享預覽圖抓不到、sitemap 對搜尋引擎無效。

**等 Zakk 確認帳號名之後全站置換就好(五個地方,一次改完)。**

### ✅ 已經處理好的

1. **`.gitignore` 沒問題** —— `_source/`(734MB,含那個 498MB 的 .ai)與
   `_dev/`(208MB)都擋掉了。實際會上線的內容 **18MB**。
2. **分享預覽圖做好了** `assets/og/og-cover.png`(1200×630,143KB),
   由 `_dev/build_og.py` 產生。
   - 原本 og:image 指向 `about-summit.webp` —— 那是 **1050×1400 的直幅**,
     所有平台都會裁成 1.91:1,人被切掉
   - **用 PNG 不用 WebP**:og:image 的 WebP 支援度到現在還是不齊
     (LinkedIn、部分 Slack 用戶端直接不顯示)
   - 字型直接用網站自己的 woff2 轉 TTF 餵 PIL(`fontTools` + `brotli` 本機都有)——
     **不能拿系統字型代打**,Archivo 的 `wdth: 112` 是這個站的招牌
3. **清掉九頁的死屬性** `data-src="assets/3d/bones/spine.svg"` + `data-flat`。
   `main.js` **完全沒有讀這兩個屬性**(grep 是空的),network log 也確認
   spine.svg 從來沒被請求過 —— 是 §47 拆掉多骨頭系統之後留下的。
   vegic 那頁的註解還寫著「這一頁是軀幹與爪」,是假的。
   清掉後實測碎片照常畫(canvas 1398×1288 有色像素正常)。
4. **全站連結檢查:10 頁、缺檔 0**。外部連結四個(framer、GitHub、IG、佔位網址)。

### 🔜 等 Zakk 決定

- **GitHub 帳號 / 實際網址**(上面那條,**這個沒確認不能上**)
- **還沒有 git repo** —— `git init` + 首次 commit + 推上去 + 開 Pages
- **6.5 MB 沒用到的圖也會一起上線**(佔 18MB 的三分之一)。最大的幾個:
  `about-portrait.webp` 1326KB、`hero-portrait.webp` 1188KB(都是換掉的舊人像)、
  `assets/3d/螢幕擷取畫面 2026-07-26 124541.png` 623KB(檔名還是中文,網址會很醜)、
  `assets/3d/bones/*.svg`(§47 之後就沒人用了)、所有 `@sm.webp` 縮圖(約 1.5MB)。
  **刪檔要先問過,沒動。**
- `works/farmily/` 與 `works/elderly/` 首頁沒連、sitemap 也沒有,但檔案還在,
  **直接打網址還是進得去**。要真的下架就把資料夾刪掉。
- `?debug` 診斷面板還在(`js/main.js` 註解寫「上線前整段刪掉」)。
  它要網址帶 `?debug` 才會出現,不影響一般使用者,留著也可以。

---

## 68. 🔴 Works 預覽圖離游標 417px —— 同一個坑第五次(2026-08-03)

Zakk:「預覽圖片可以離滑鼠近一點」。**這不是偏好問題,是 bug。**

### 真兇:`.works` 的進場動畫留著 transform

預覽圖是 `position: fixed`、由 JS 餵 `e.clientX/clientY`(視窗座標)。
但 `.works`(那個 `<ul>`)身上有 `.reveal` 進場動畫的 `transform`,
而且結束後停在 `matrix(1,0,0,1,0,-2.852)` 而不是 `none`,
另外還有一個 `filter: blur(0px)`——**兩者都會建立 containing block**。

> **祖先只要有 transform / filter / perspective / will-change(連 identity、
> 連 blur(0px) 都算),`position: fixed` 就改用那個祖先當基準,不再對齊視窗。**

實測(視窗 1398×1288,游標在 x=479 / y=584):

| | 之前 | 現在 |
|---|---|---|
| 左緣離游標 | 84px | **18px** |
| 垂直偏移 | **417px** | **0px**(正對齊) |

### 修法:把預覽圖搬到 `<body>` 底下

不能靠「拿掉 `.works` 的 transform」—— 那是進場動畫,拿掉等於把動畫砍了。
也不該在 JS 裡減掉祖先的偏移(祖先一改又要重算)。
直接讓它不要有會製造 containing block 的祖先最乾淨:

```js
for (var n = 0; n < rows.length; n++) {
  var tn = rows[n].querySelector('.works__thumb');
  thumbOf[n] = tn;                       // 搬走之後就不是子元素了,要自己記
  if (tn) document.body.appendChild(tn);
}
```

⚠️ 搬走之後 `.works__row.is-hover .works__thumb` 這個選擇器就選不到了,
顯示狀態要改掛在預覽圖自己身上(`.works__thumb.is-hover`)。
舊那條規則留著當備援(JS 沒跑時)。

順手把跟隨手感調緊:`OFFSET_X` 28 → **18**、`lerp` 0.38 → **0.5**。

### 🔴 這是同一個坑第五次

§18 clip-path + IntersectionObserver、§53 `--lock`、§58 `position: fixed`、
§62 `will-change`、這次是**進場動畫的 transform**。

**規則(貼在這裡,下次先查):做「照視窗座標定位」的東西之前,
先把祖先鏈掃一遍 —— `transform` / `filter` / `perspective` /
`backdrop-filter` / `will-change` / `contain` 任何一個非預設值都會把基準換掉。**
掃描的程式碼留在 §68 這次的量測裡(往上找祖先、比對 computed style)。

⚠️ **量測注意**:用 `dispatchEvent` 合成 mouseenter/mousemove 測不準 ——
真實游標的 mouseleave 會跟合成事件打架,量到的是「已經被關掉」的狀態
(第一次量到 opacity 0、transform 空白,差點去翻 CSS 權重)。
**要用真的游標**(`computer` 的 hover),才是使用者看到的東西。

**實測**:游標移到第 2 列 → 預覽圖切成 Kaxabu、左緣 +18px、垂直 0px、
同時只有一張可見;點下去正常進 `/works/kaxabu/`;無 console 錯誤。
JS 改完有先驗語法(§43 的鐵則)。

---

## 67. Zakk 砍掉三列 + 碎片不再擋圖面(2026-08-02)

### ① 首頁只留八件(Zakk 指定:「把 05 跟 08 刪掉,11 也刪掉,那個沒做好」)

拿掉 **Let's Have Farm!**、**Elderly Residence**、**Terra Studio**。

```
01 New Era Traffic System   ↗ 外部
02 Kaxabu Tribe Identity    → works/kaxabu/
03 VEGIC                    → works/vegic/
04 CATCHU                   → works/catchu/
05 The Little Mermaid       → works/mermaid/
06 Wooden Stool             → works/antique/
07 Dragon Skull             → works/dragon/
08 Sketches & Forms         → works/sketches/
```

**八列全部有內頁,沒有一列是死的。**

⚠️ **`works/farmily/` 與 `works/elderly/` 的檔案沒有刪**,只是首頁不連過去、
sitemap 也拿掉了。要放回來只要把清單那兩列加回去 —— 沒有先問就刪掉檔案不對。

### ② 列高收回 67px

§66 ⑥ 給的三個選項裡 Zakk 選了 56px,但那是為了讓**十一列**不被矮螢幕裁掉。
他隨後把清單砍成八列,**這個理由就不存在了**:
八列在 14px 內距(列高 67)下內容高只有 **619px**,連 640px 的視窗都放得下。
所以改回 67px —— 收內距是有代價的(作品名是這一幀唯一的大字),沒必要就不要收。

實測:八列 619px、視窗高 1288 時上下各留 335px。

### ③ 🔴 碎片會從線稿的空隙透上來(Zakk:「不要讓碎片擋到圖」)

這是 §28「圖面直接印在紙上」的副作用,而且是**一開始就寫在註解裡當成優點**的
(「背景碎片也透得過來」)—— 但實際看,細線 + 碎片混在一起,圖就讀不出來了。
渲染圖沒這個問題,它本來就是不透明的照片。

修法:`.plate--drawing::before` 墊一層**紙色**(就是頁面底色,所以看不出多了東西,
只是碎片被擋在後面)。

```css
.plate--drawing { position: relative; isolation: isolate; }
.plate--drawing::before {
  content: ""; position: absolute; inset: -3%;
  z-index: -1; background: var(--paper);
  opacity: 0.93; filter: blur(22px); pointer-events: none;
}
```

⚠️ **兩個一定要注意的地方:**
1. **`isolation: isolate` 不能省** —— 不然 `z-index: -1` 會掉到 `#main` 後面
   (`#main` 是 z-index 1),整塊紙就跑到碎片**後面**去了,等於沒做。
2. **邊緣一定要散開、而且不能做滿。** `.page-atmos` 那層漸層的 alpha 只有
   0.045–0.085,一塊硬邊的實心方塊會把它切出一條看得見的界線。
   用 `blur(22px)` + `opacity: .93`,實際看不出有墊東西。

**實測**:CATCHU 一樓平面、antique 的平面/燈具/兩張室外立面 ——
圖上完全沒有碎片,也看不到墊片的邊。渲染圖不受影響(只作用在 `.plate--drawing`)。

---

## 66. 剩下六件內頁做完 —— 九件全部有內頁了(2026-08-02)

Kaxabu、Dragon Skull、The Little Mermaid、Elderly Residence、
Let's Have Farm!、Sketches & Forms 一次做完。首頁 **11 列全部可以點進去**
(只有 Terra Studio 還沒有連結)。

### ① ⚠️ Elderly 的五張圖面整組是躺著的

原檔版面上這五張是轉 90° 排的(標題「一樓平面配置圖」是直的、房間名整片側躺),
抽出來當然也是躺的 —— 而且**一直都是**,只是之前沒人打開看過。
`build_work_images.py` 加了 `ROTATE`,值是逆時針角度(跟 PIL 的 `rotate` 一致),
`elderly-01`–`05` 各轉 90°。轉完 1132×1600 變成 1600×1132,
HTML 的 `width`/`height` 要跟著改。

### ② 白底圖的第三、四種情況

| 圖 | 處理 | 為什麼 |
|---|---|---|
| `mermaid-01` 紙燈、`dragon-04` 頭骨、`farmily-02` 爆炸圖 | `CUTOUT` 去背 | 乾淨白底 + 單一物件 |
| `farmily-05` / `07` 享食籃 | **只能 `TRIM`** | **渲染在地上留了一塊實心黑影,白底挖掉之後黑影會單獨浮在紙上,比白框還難看** |

去背的判斷準則現在很清楚了:**看物件本身有沒有接近白的部分**
(CATCHU 的白牆、farmily-07 的白色籃子),以及**地上有沒有實心陰影**。

### ③ 反白設計不能去背

`extract_vector_art.py` 的 `CROPS` 多了第四個欄位 `cut`。
Kaxabu 的標準字有淺底與**深底反白**兩版 —— 反白版的深色底板**就是設計本身**,
去背之後白字會直接消失在紙色上。同理以後遇到深色 mockup 都要關掉去背。

順手抽出來的還有 Farmily 的標準字與兩組 LOGO 組合(p004)。

### ④ 🔴 量測陷阱又來一次:瀏覽器面板沒顯示 = IntersectionObserver 不會動

量 Kaxabu 頁時拿到「17 個進場元素 **0 個**觸發、6 張 lazy 圖沒載入」,
差點以為 main.js 在這一頁壞了(還去翻了 reveal 的程式碼)。
真相是**預覽面板當時沒有顯示**,渲染器不合成畫面 →
IntersectionObserver 的 callback 不會呼叫、`loading="lazy"` 也不會觸發。
面板一顯示,同一頁馬上 17/17。

**以後量進場 / lazy 相關的東西,先確認畫面真的有在畫**(截一張圖最快)。
這跟 §43「JS 沒跑時 rAF 照樣 60fps」是同一類的假數據。

### ⑤ 圖說與圖檔的個案

- **Elderly 的渲染圖說全錯**:自動抓的把「建築正面」套在玄關、「身障者設計廁所」套在臥室、
  「休閒庭院」套在書房。看圖對回 p031/p032 的原始清單重寫。
- **`sketch-product-thumbnails-02` 拿掉了**:原檔只有 302×403(從 IG 存下來的),
  放進半欄會被放大到 631px 變糊,`_source` 裡也沒有更大的來源。
  同內容的 `-01` 是 1349×1887,夠用。
- **Dragon 開場換大圖**:原本用去背的 `dragon-04`,但它只有 286×298,
  在 1398px 寬的版面上太小。改成 `dragon-02` 全寬,
  去背的那張移到最後、**貼著「首頁碎片就是這顆頭骨」那句話旁邊** ——
  `assets/3d/dragon.webp` 確實就是這張,這是事實不是文案。
- **Sketches 刻意沒有文案**(Zakk 決定):作品集沒有這一頁,沒有他寫過的字可以用,
  編一段設計理念等於替他發言。

### ⑥ 首頁 11 列的實測(要 Zakk 決定)

內容高(標題 + 11 列)= **819px**,每列 **67px**。

| 視窗高 | 結果 |
|---|---|
| 1288(Zakk 的螢幕) | 放得下,上下各留 235px |
| 900 | 放得下,上下各留 41px |
| 800 | 溢出 19px(裁掉 0.3 列) |
| 740 | 溢出 79px(裁掉 1.2 列) |
| 700 | 溢出 119px(裁掉 1.8 列) |
| 640(13 吋筆電) | **溢出 179px,裁掉 2.7 列** |

縮列高的試算:67 → **56px** 內容變 698、→ **48px** 變 610、→ **44px** 變 566。
`.frame__pin` 是 `overflow: hidden`,溢出的部分直接看不到,**不會有捲軸**。

### ⑦ 順手修掉的 CLS

首頁預覽圖的 `width`/`height` 全部寫成 1200×800,但實際檔案是
1200×675 / 1029×1200 / 900×1200 / 1199×1200 —— 標錯會讓版面在圖載入時跳一下。
改成用實際尺寸(重建清單時一併查檔算出來的)。

**實測**(每一頁都跑過:捲到底 → 回頭數)

| 頁 | 圖 | 進場元素 | 頁高 | 橫向捲動 |
|---|---|---|---|---|
| kaxabu | 7/7 | 17/17 | 6164 | 0 |
| dragon | 4/4 | 13/13 | 3884 | 0 |
| mermaid | 2/2 | 9/9 | 4136 | 0 |
| elderly | 12/12 | 19/19 | 7227 | 0 |
| farmily | 9/9 | 18/18 | 6018 | 0 |
| sketches | 4/4 | 9/9 | — | 0 |

無 console 錯誤;持續捲動中位數 **16.7ms** / p95 **16.9ms** / 最慢 **17.5ms** / 掉偵 **0**。
首頁 11 列的 10 個連結全部正確、11 張預覽圖全部載入。sitemap 補齊九頁。

**🔜 還沒做**
- **首頁列高**(見 ⑥,等 Zakk 決定)
- og:image 要一張 1200×630 橫幅(現在指向直幅的山頂照,分享會被裁)
- Terra Studio 還沒有連結

---

## 65. 內頁第 2 件:CATCHU + 向量圖形也抽得出來(2026-08-02)

### ① 🔴 LOGO 一直都在,只是抽圖工具看不到它

`works-images.json` 裡 catchu 的 20 張全部來自 p017–021(平面、剖立面、渲染),
**品牌那一頁(p016)是 0 張** —— 我原本以為那頁沒有素材,打算把品牌章寫成純文字。

實際把 p016 畫出來看:**三組 LOGO 組合、色彩計畫色票、標準字全都在上面**。
`_dev/extract_pdf.py` 抽的是「嵌在頁面裡的點陣圖」,而 LOGO 是**向量**,
原檔裡根本沒有點陣圖可抽。

新增 `_dev/extract_vector_art.py`:把整頁 render 出來再裁一塊,向量就變成圖了。
裁切框寫在 `CROPS`,單位是 REF_DPI=110 那張圖的像素(看著畫面量最快),
實際用 OUT_DPI=440 輸出。底色 #EEEDED 跟網站紙色 #EFEDE8 幾乎一樣,去背後接得上。
**其他件的標準字 / LOGO / mockup 也可以用同一支拿。**

### ② ⚠️ 白底的圖有三種,不能一套處理打天下

| 情況 | 處理 | 為什麼 |
|---|---|---|
| 線稿(平面、立面) | `inkify` 去背 | 黑線白底,去背後直接印在紙上 |
| 白底實物照(椅凳、LOGO) | `knockout` 去背 | 照片,顏色要留著,inkify 會變灰影 |
| **白底彩現(CATCHU 四張立面)** | **`trim_white` 只裁白邊** | **房子的牆本身就是白的 —— 去背會把牆一起挖掉,房子破洞(實測過)** |

第三種是這次才發現的。原本想比照椅凳去背,做出來白色戶外樓梯與白牆整片消失。
改成只裁掉四周白邊:800×450 → 733×196,白框只剩房子那一塊,
從「白框裡一棟小房子」變成一張印刷版。

⚠️ 裁完**圖檔尺寸會變**,HTML 的 `width`/`height` 要跟著改,不然 CLS 會跑掉。
也因為長向兩張變成寬扁橫幅、窄向兩張接近方形,**不能再硬排 2×2** ——
長向各佔一列、窄向並排。

### ③ 新增三個小元件(都在 work.css)

- `.legend` 平面圖的房間編號對照表 —— 圖上的編號在螢幕尺寸下小到看不清,
  對照表才是讓平面圖讀得懂的東西。⚠️ 要限寬:不限的話 1398px 寬會攤成 **7 欄**,
  每項離很遠,不像一張表。設 `max-width: 860px` → 5 欄。
- `.swatches` 色彩計畫 —— 色塊是示意,**底下那行 CMYK 才是原檔的值**,所以數字要看得見。
  色票的 RGB 是從 p016 render 出來直接取樣的(#D38991 / #FDF9E8 / #3F3B3A),不是用公式換算的。
- `.pull` 標語 —— 一頁只放一句(`catch your eyes.`)。

### ④ ⚠️ §63 說「11 列一幀裝不下」是錯的

實測(視窗高 1288px):每列 **67px**,8 列佔 618px、上下各留 335px。

| 列數 | 內容高 | 上下各留 |
|---|---|---|
| 8 | 618 | 335 |
| 9(現在) | 686 | 301 |
| 11 | 819 | 235 |

**11 列在 Zakk 的螢幕上裝得下。** 真正的問題在矮螢幕:
內容高 819 表示視窗要 ≥ 860px 才不會被 `overflow: hidden` 裁掉,
13 吋筆電(視窗約 640px)**連現在的 9 列都已經滿了**。
等最後兩件(Elderly、Farmily)做完再一起決定要不要縮列高。

### ⑤ 渲染圖圖說一張一張校正過

自動抓的圖說會把同一頁的多張套同一個名字(§28 ③ 就記過)。
實際看圖後改:`10–13` 是四向立面彩現、`14 15` 外觀、`18` 入口與點餐、
`19` 貓咪旅館與遊樂區、`17` 圓桌區、`16` 2F 戶外用餐區、`20` 頂樓草皮座位區。
**立面彩現不標東西南北** —— 對照四張立面圖可以推,但推錯就是把他的圖標錯,
改用看得見的特徵(入口格柵側 / 臨路長向 / 戶外樓梯側 / 露臺側)。

**實測**:21 張全載入、34 個進場元素全觸發、無橫向捲動、無 console 錯誤;
頁高 13167px;手機 375px 下對照表 2 欄、無橫向捲動。
首頁第 04 列已接上 `works/catchu/`,原本的 04–08 順延成 05–09,sitemap 也補了。

**🔜 剩下 6 件**:Kaxabu、Dragon Skull、The Little Mermaid、
Elderly Residence、Let's Have Farm!、Sketches & Forms。

---

## 64. 內頁第 1 件:Wooden Stool + 文案來源解決了(2026-08-02)

### ① 🔴 內頁文案不用編了 —— 原檔裡有 Zakk 自己寫的字

之前一直把「文案」當成待解問題(§28 ③「不能編,要 Zakk 給」)。
其實 `_source/portfolio/作品集 NEW2 (1).ai` 每一頁都有設計理念/痛點/功能/房間清單,
`fitz` 的 `page.get_text()` 直接讀得到。`_dev/extract_pdf.py` 只抓了「┌ xxx」圖說,
**沒抓內文** —— 這是它的漏,不是原檔沒有。

九件的文字都抽出來了(指導老師、組員名單也在)。**內頁一律用他自己的原文,
英文段落只是翻譯,不是新寫的。** 唯一沒有來源的是 Sketches & Forms(作品集沒這頁),
Zakk 決定**只放圖不寫文案**。

### ② 兩個素材缺陷(自動判斷判錯,看圖才發現)

- **antique-05 / 06 判成渲染圖** → 留著白底,在米灰紙上是一塊白方塊。
  它們確實是線稿,但一張裡並排兩幅、又有大面積格柵剖面線,
  中間調衝過 `classify()` 的 0.20 門檻。已寫進 `FORCE_KIND`。
- **椅凳實物照是白底照片** —— 不能 inkify(照片會變灰影),但白底在紙上像貼紙。
  新增第三種處理 `knockout()`:**只挖白底、顏色留著**,寫在 `CUTOUT` 集合裡。
  柔邊 lo=225 / hi=250(實測白底佔 63–67%、過渡帶只有 0.24%、木頭最亮遠低於 225)。
  順手用 alpha 的 `getbbox()` 切掉四周空白 —— 原檔 1200×1600 裡物件只有 1147×1107,
  不切的話物件會離版面的線很遠,看起來像飄著。

⚠️ **`build_work_images.py` 帶 slug 參數跑會把 works-images.json 寫成只剩那一件**
(`out` 只收有被過濾到的)。所以一律**全部重跑**。已驗證是決定性的:
重跑後其他八件的 md5 完全沒變。

### ③ 版面:兩個決定

- **開場圖用實物照,不是平面圖。** VEGIC 是空間案所以開場用圖面;
  這件 Zakk 指定「重點在椅子本身」,主角就該先出現。
- **⚠️ 限高不能用 `vh`。** 一開始寫 `max-height: 64vh`,實測在 720 高的視窗是 461px、
  在 1306 高的視窗會變 836px —— 一件實物該有多大跟視窗多高無關。改成固定 `620px`。
- **去背的東西置中放在一大片紙上會「飄」**,沒有著力點。
  開場圖改**靠左** + 底下一條貫穿版面的細線當檯面
  (`.plate--object` 的註解本來就寫著這一招,只是沒用在大圖上)。

### ④ 圖面取捨(Zakk:「房子/室內簡單帶過就好」)

15 張只用 8 張:`01 02`(椅凳)`12`(在圖書館裡)|`04 03`(平面 + 燈具)`05`(室外立面)`11 13`(渲染)。
**沒放的 7 張**:`06`(室外背/左立面)、`07–10`(四張室內立面)、`14 15`(兩張外觀渲染)。
要加回來隨時可以,但那會讓房子搶掉椅子的份量。

**實測**:8 張全載入、17 個進場元素全觸發、無橫向捲動、無 console 錯誤;
持續捲動中位數 **16.7ms** / p95 **17.0ms** / 最慢 **17.2ms**;
桌機開場圖 642×620、手機 375px 下 335×335;VEGIC 頁不受影響(19 張 / 28 個進場全過)。

首頁第 05 列已接上 `works/antique/`(自動帶 → 箭頭),sitemap 也補了。

**🔜 剩下 7 件**:CATCHU、Kaxabu、Dragon Skull、The Little Mermaid、
Elderly Residence、Let's Have Farm!、Sketches & Forms。

---

## 63. ✅ 作品 ↔ 素材對應表(Zakk 2026-08-02 確認,**做內頁前先看這張**)

素材其實**早就備好了**:`assets/works/` 九個資料夾、182 張已轉檔的圖。
卡住的一直是「首頁名稱對不到資料夾」。Zakk 確認如下:

| # | 首頁名稱 | 素材資料夾 | 張數 | 內頁 |
|---|---|---|---|---|
| — | New Era Traffic System | **無**(外部連結 cyd2025.framer.website) | — | 不做 |
| — | Kaxabu Tribe Identity | `kaxabu` | 10 | 要做 |
| — | VEGIC — Near-expiry Produce | `vegic` | 38 | **已完成** |
| — | The Little Mermaid | `mermaid` | 4 | 要做 |
| — | Wooden Stool | **`antique`**(古董,Zakk:「antique 跟 Wooden Stool 是一樣的」) | 30 | 要做 |
| — | **CATCHU 喵一眼**(新增) | `catchu` | 40 | 要做 |
| — | **Elderly Residence**(新增) | `elderly` | 24 | 要做,**簡單提概念就好** |
| — | **Let's Have Farm!**(新增) | `farmily` | 14 | 要做,**簡單提概念就好** |
| — | Dragon Skull | `dragon` | 8 | 要做 |
| — | Sketches & Forms | `assets/projects/sketches` | 5 | 要做 |
| — | Terra Studio | 無(網站作品) | — | 外部連結 |
| — | ~~佐佐木賢實習~~ | `sasaki` | 14 | **不放**(先前決定) |

**內頁的側重(Zakk 2026-08-02 指定):**
- **Wooden Stool(antique)**:重點在**椅子本身**,房子/室內的部分簡單帶過就好。
- **Elderly Residence、Let's Have Farm!**:簡單提概念即可,不用完整展開。
- 其餘(Kaxabu、Mermaid、CATCHU、Dragon Skull、Sketches)照 VEGIC 的完整做法。

**⚠️ ~~首頁清單會從 8 列變 11 列 —— 一幀裝不下(實測 8 列剛好滿)~~ 這句是錯的,見 §65 ④:
在 1288px 高的視窗每列 67px,11 列佔 819px 還有餘裕。真正的限制是矮螢幕。**
做之前要先決定:縮列高、還是首頁只放精選+其餘進彙整頁(§54 做過又拆掉,
但那時是 9 列硬塞;11 列的狀況不同,值得重新評估)。

**✅ 2026-08-02:這張表上「要做」的九件全部做完了,見 §64 / §65 / §66。**

**其他還沒做**:og:image 要一張 1200×630 橫幅(現在指向直幅的山頂照,分享會被裁)。

---

## 62. 作品名稱、預覽圖救回、Contact 位置(2026-08-01)

**① 作品名稱改中文**(Zakk 指定)
01 Works Collection → **新型態交通號誌系統**
03 Commercial Space / Sugar Reduction → **VEGIC 即期蔬果**
IG → `instagram.com/zakktseng`。Sasaki 那筆不用中文(他說沒關係)。

**② 🔴 預覽圖不見了 —— 是我在 §55 弄丟的**
把八件從彙整頁搬回首頁時,我複製的是**彙整頁的純文字列**,
那份沒有 `<figure class="works__thumb">`。八列的預覽圖就這樣全部消失,
而且過了好幾輪都沒人發現(我自己也沒發現)。
已依作品重新掛回 assets/projects/works/ 的圖,沒有圖的三件用 `--empty` 佔位。

**教訓:搬移內容時,要比對「搬過去的」和「原本的」欄位數量,
不是只看名字對不對。**

**③ ⚠️ `will-change` 也會變成 fixed 的定位基準**
`.frame__sheet` 舊規則有 `will-change: clip-path, opacity, transform`。
它和 transform / filter 一樣會建立 containing block ——
Works 的游標預覽圖是 `position: fixed`、JS 給的是視窗座標,基準一換就全錯。
已改成 `will-change: auto`。
**這是「我改的東西不是最終生效的東西」第四次出現。**

**④ Contact 往下拉** `margin-top: clamp(48px, 7vh, 96px)`,不再被直幅照片壓到。

---

## 61. Contact 併進 Photos + 照片真兇 + 開場每次都播(2026-08-01)

**① 🔴 「照片超亮」的真兇是 CSS,不是圖檔 —— 我重做了三次圖都沒用**
```css
.about__portrait img { filter: brightness(1.75) contrast(0.9) saturate(1.05); }
```
這條是為**上一張偏暗的人像**寫的。換成山頂那張之後它把照片硬提亮 75%,整片洗白。
Zakk 連續三次說「照片還是一樣阿」,我卻一直在調 Pillow 的參數。

**教訓:當使用者說「改了沒用」,先確認是不是有別的東西蓋在上面,
不要急著重做同一件事。** 這跟 §57 的 CSS 權重、§53 的 --lock 是同一類問題:
**我改的東西不是最終生效的東西。**

**② 開場改成每次重新載入都播**
「同一次瀏覽只播一次」在調整開場時很礙事(Zakk:「我還是看不了載入動畫」)。
現在只有 `document.referrer` 指向 `/works/` 時才跳過。

**③ Contact 併進 Photos**(Zakk:「photo 也沒有很滿,合併就好」)
我先做過一版兩欄式 Contact(標語 + 信箱 + 事實欄),把整幀撐滿了 ——
但 Zakk 說「沒有很好看」,而且那些內容是**為了填空長出來的,不是真的需要**。
現在回到最初的簡版(Contact + Email / Instagram / GitHub),接在照片帶下面。
全站從 5 幀變 **4 幀**,導覽從 4 項變 3 項,vegic 內頁導覽同步。

**④ 拿掉我編的內容**:「Within a day」(回覆速度)是我自己寫的,已移除。
About 的 Download CV 指向 `assets/cv/zakk-tseng-cv.pdf`,**這個檔案存在**,連結正確。

**🔜 Works 那一幀「只有字跟線,很無聊」——Zakk 明確指出,還沒做。**
既有素材:每一列裡已經有 `.works__thumb`(滑過時跟著游標浮現)。
方向是把它變成右側常駐的預覽面板,用掉右半邊那片死區,
而不是再加裝飾。**動到 JS 的游標追蹤邏輯,要小心。**

---

## 60. 底色換暖米灰 + 五項修正(2026-08-01)

**① 底色 `--paper` #F4F6F7(冷紙白)→ #EFEDE8(暖米灰)**
Zakk:「周遭有點太空,可能因為網站是純白,如果是有點米灰呢」—— 他的判斷是對的。
冷白會把留白讀成「還沒做完」,暖灰讀起來是「刻意留的」;
碎片是冷灰藍,壓在暖底上比壓在冷底上更清楚。

**⚠️ 換底色一定要重測對比。** `--stone #646E75` 在新底色只剩 **4.45**(低於 AA 4.5)。
改成 **#606A71 = 4.73**。`--ink-dsp` 13.69 沒問題。

**② About 照片改回原圖**(Zakk:「直接用原圖,這張還是不行」)
不再做任何亮度/對比調整,只裁切縮放。**他的眼睛比我的數值可靠。**

**③ 右側頁籤跟內容的線打架**
`.side-nav` 是 fixed 在視窗右緣,內容分隔線一路拉到版面右邊就疊在一起。
`#about .about, #works .works { padding-right: 76px }`(只在 ≥721px)。
實測:線右緣 1359 < 頁籤左緣 1391。

**④ Works→About 重疊** `OUT_LEN` 1.10 → **0.85**(退場更快讓位)。
實測最大重疊 0.37 → **0.20**。

**⑤ 環繞粒子** 0.62 → **0.82**(Zakk 連續兩次說還是太少)。

**⑥ 開場可以重播**:網址加 `?intro`(例:`localhost:8765/?intro=1`)強制重播,
不用關分頁。

---

## 59. 捲動距離、粒子量、照片、經歷、載入殘影(2026-08-01)

**① 幀高 200svh → 150svh**(Zakk:「要滑好多圈才到下一頁」)
一幀從 2576px 降到 1932px,全站 13000 → 9835px。
⚠️ **進出場的數字不能寫死**:單位是「釘住那段的捲動距離」(total = 幀高 − 視窗高),
幀高一改,相鄰兩幀的 p 差距 `OFF = 幀高 / total` 就跟著變(200svh 時是 2,150svh 是 3)。
現在每一幀依自己的 OFF 算。

**② 交接曲線:退場快、進場慢**
兩邊一樣長 → 同時停在 50% → 兩整頁疊在一起(Zakk 不要)。
兩邊都短 → 出現全空的一瞬間(實測掉到 0.08)。
`OUT_LEN = 1.10, IN_LEN = 2.00` → 時間上重疊但透明度錯開。
實測:最空 0.39、最大重疊 0.37。

⚠️ **`p` 的下限要比 IN_LEN 更遠**,否則遠處的幀卡在「進場一半」永遠淡不掉(重疊衝到 0.58)。

⚠️ **量測陷阱又出現一次**:掃描時只等 140ms,平滑捲動還在追,
回報的 scrollY 和畫面狀態對不上 → 誤判重疊 0.53。等到 420ms 才是真的。

**③ 拿掉載入殘影**(Zakk:「粒子有種殘影特效,太多太亂,很不好看」)
原本載入中不清空畫布、改蓋一層半透明底色做拖尾。3600 顆碎片各拖一條尾巴會糊成一團。
改回每偵 `clearRect`。

**④ 首頁以外的環繞粒子** 0.42 → **0.62**。

**⑤ About 照片壓暗**:山頂逆光原圖高光整片洗白。
`Brightness 0.82 + Contrast 1.14`,天空和雪地回到有層次的灰。

**⑥ 經歷加中文大標**(只有機構名,職務描述維持英文):
國立雲林科技大學 / **endesign 一森室內設計**(原本寫 "Interior Design Studio" 是佔位字)。
⚠️ Sasaki Ken 那筆 Zakk 沒給中文,**不要自己編**。

---

## 58. fixed 帶出來的兩個後遺症 + 載入改等速時間軸(2026-08-01)

**① ⚠️ 首屏頭骨不見了 —— 又是「透明 ≠ 不存在」**
內容改成 `position: fixed` 之後,**每一幀的文字都疊在視窗上**,即使 opacity 是 0 也一樣有位置。
碎片的「避開文字」照單全收 → 整個畫面都被當成有字 → 碎片只剩 110 點。
修法:只算 `data-live="1"` 那一幀的文字。
```js
var sheet = els[i].closest('.frame__sheet');
if (sheet && sheet.getAttribute('data-live') !== '1') continue;
```
修完:墨點 110 → **9894**。

**🔴 這是同一個坑第三次踩到**(§18 clip-path + IntersectionObserver、§53 --lock、這次 fixed)。
**只要改變元素的定位方式,就要回頭檢查所有「照位置算」的邏輯。**

**② ⚠️ 交叉淡出不能做滿**
§57 我讓 out [1,2] / in [-1,0] 完全互補,結果兩幀在中點各 50% ——
兩整頁文字疊在同一個位置,Zakk 回報「切換頁的時候內容都會互相重疊」。
內容已經不會滑了,所以只需要很短的交接:`IN_START = -0.60, OUT_END = 1.55`。
實測:過場中「第二亮的幀」最高只到 **0.05**。

**③ 載入動畫改成等速時間軸**(Zakk:「碎片的動畫很不順很奇怪,骨頭聚攏的過程不夠明顯」)
原本 `shown += (target - shown) * 0.028` 有兩個毛病:
- target 是階梯式的(字型好了跳一段、圖片好了又跳一段)→ 碎片一段一段衝
- 指數追趕 → 前段衝很快、後段用爬的,最該看清楚的「合體」反而最慢

改成動畫走自己的等速時間軸(`GATHER_MS = 2200`),真實載入只決定「可不可以收尾」:
沒載完就停在 92% 等,載完再用同樣速度走完。ease 換 easeInOutCubic,
碎片先慢慢漂 → 中段明顯往中心收 → 輕輕落定,聚攏有頭有尾。

---

## 57. 內容改成 fixed —— 「播影片」總算真的成立(2026-08-01)

Zakk 第五次回報:「還是看得出上下滑」。

**根本問題:前四版都在「用位移抵銷位移」。**
pin 跟著捲動走,我再用 transform 推回去。只要抵銷差一點點就看得出來,
而且放開鎖的那一段一定會有加速感。**只要內容還在捲動流裡,就一定滑得出來。**

改法:`.frame__sheet` 直接 `position: fixed`,完全不隨捲動移動。
`.frame` 仍是 200svh,但它現在純粹是「這一幀要播多長」的捲動長度。
捲動只做一件事:決定哪一幀是亮的。
實測:連續滾 18 段,文字位移 **18 次全部是 0**。

**踩到的三個坑(都是 fixed 帶出來的):**
1. ⚠️ **離太遠的幀不能只是 `continue`** —— `--in` 會停在預設值 1,
   五幀全部疊著同時顯示。要明確設成隱藏再跳過。
2. ⚠️ **選擇器權重**:`.frame__sheet > *` 是 (0,1,0),輸給 `.reveal.is-in { opacity: 1 }` (0,2,0)。
   `--in` 明明是 0,子元素 opacity 還是 1。改成 `.frame__pin .frame__sheet > *`。
3. fixed 會脫離 `.section` 的 `max-width` / `padding`,要自己補
   `margin: 0 auto; max-width: var(--maxw); padding: 0 var(--gutter)`。

**交叉淡出改成做滿**:相鄰兩幀的 p 永遠相差 2,所以 out 走 [1,2]、in 走 [-1,0] 剛好互補。
實測全站掃描:總可見度 **最低 1.00**,沒有任何一刻是空的。

**🔜 Zakk 說「載入動畫也需要調整」,還沒問清楚是哪裡。**

---

## 56. ⚠️ 修卡頓時不要順手把「影片感」一起改掉(2026-08-01)

§55 我為了修「卡卡的」,一次改了三件事:
1. `--lock` 硬開關 → 乘上 `vin * (1 - vout)` 平滑放開  ← **只有這個是對的**
2. 鎖的力道 1.0 → 0.85
3. 定格 1.30 幀 → 0.95 幀

Zakk 立刻回報:「又出現往下滾的感覺了,沒有剛剛播影片的氛圍。」

**內容釘死不動正是「影片感」的來源,不能拿它去換順暢。**
真兇從頭到尾只有一個:硬開關造成的 698px 瞬間跳。
2 和 3 都是我順手加的,結果把使用者要的東西一起改掉了。

已還原成 `lockF = 1.0 * vin * (1 - vout)`、`IN_END = 0.00, OUT_START = 1.00`。
實測:定住不動 6 段、**大跳 0 次** —— 影片感和順暢兩個同時成立。

**🔴 教訓:一次只改一個變因。** 三個一起改,回報「變糟了」的時候根本分不出是哪個。
這條在 CLAUDE.md 裡本來就寫著(「不要一次改太多,一步一步來」),是我沒遵守。

---

## 55. 八件全放回首頁 + 修「滾不動」的手感(2026-08-01)

**① 彙整頁作廢**(Zakk:「有點多餘,空間其實放得下所有作品」)
他是對的 —— 1306px 高的視窗,八列到底部才 962px。`works/index.html` 已刪除,
八件全部放回首頁(Residential Interior 仍然不放)。
**教訓:先量再砍內容。** 我看到「只顯示四列」就直接提了三個縮減方案,
但那次是在過場中間量的,不是穩定狀態。

**② ⚠️ 「換頁卡卡的」不是效能問題**
實測連續滾動 157 偵:中位數 16.7ms、p99 17.0ms、**掉偵 0**。
真正的原因是手感 —— 量「滾輪每滾 180px,畫面上的字實際移動幾 px」:
```
舊: -74, 0,0,0,0,0,0, -74, -27,-27,-27, -698, -180, -180
                                          ↑ 這個跳就是「卡」
```
兩個毛病:
- `--lock` 用硬開關(`live ? 鎖 : 不鎖`),幀一失去資格就瞬間解鎖 → 跳 698px
- 定格區佔 1.30 幀 ≈ 1700px,滾輪滾半天畫面不動 → 身體覺得「滾不動」

修法:
```js
var lockF = 0.85 * vin * (1 - vout);   // 淡出到哪就放開到哪,沒有斷點
IN_END = -0.10, OUT_START = 0.85;      // 定格 1.30 幀 → 0.95 幀
```
修完:`-74, 0×6, -86, -112, -197, -246, -213, -180…` —— **大跳 0 次**,平滑加速。

**③ 拿掉 `filter: blur()`**
`.frame__sheet > *` 的「對焦感」每一偵都要對整頁文字重算高斯模糊。
opacity / transform 可以完全交給 GPU 合成,blur 不行。
質感差一點但順暢優先;真正的對焦感現在由碎片在做。開場字母的 blur 保留(只有四個字,便宜)。

**🔜 定格區還有 6 段是完全不動的(約 1080px)。** 那是「像播影片」的定格,是設計不是 bug,
但如果 Zakk 覺得還是太久,調 `OUT_START`(現在 0.85)往下就會更短。

---

## 54. Works 改成首頁精選 + 彙整頁(2026-08-01,已作廢 —— 見 §55)

原本九件全列在首頁,一幀只裝得下四列,其餘被 `.frame__pin { overflow: hidden }` 裁掉。
Zakk 選 C:首頁只放精選,其餘收到彙整頁;Residential Interior 整個拿掉(他的判斷:小作品)。

**首頁四件**,順序刻意走完 hero 那句 "From space to screen":
```
01 Kaxabu Tribe Identity           Brand Identity
02 Commercial Space / Sugar Red.   Spatial · Packaging   ← 唯一有內頁的
03 The Little Mermaid              Lighting
04 Terra Studio                    Web / Dev
   All works                       Archive · 08
```
品牌 → 空間 → 產品 → 螢幕,四件剛好把他自稱的守備範圍走一遍。

**新頁 `works/index.html`**:八件全列,可以正常往下捲(不是「幀」)。
不放骨頭碎片 —— 這一頁的工作是快速掃過全部,背景有東西動反而干擾。

**⚠️ 新頁忘了加 `reveal` → 標題整個看不見**
`.section__title` 預設是 `clip-path: inset(0 0 110%)`,靠 IntersectionObserver
加 `is-in` 才解開。新頁沒有 `reveal` 類別 → 觀察器不理它 → 永遠是裁掉的狀態。
**新增任何頁面時,section__head / 主要區塊都要記得帶 `reveal`。**

**🔜 還沒做**:其餘七件的內頁(目前只有 vegic)。彙整頁先用純文字列,有內頁再掛連結。

---

## 53. ⚠️ --lock 把看不見的文字搬到首屏,挖破頭骨(2026-08-01)

症狀:首屏頭骨變成右上一道斜斜的碎片流,中間有幾條淡淡的橫帶。

原因:`--lock`(把內容推回畫面中央的位移)每一幀都算,
**包含還沒輪到、離畫面好幾千 px 的幀**。它們被整塊拉到首屏中央 ——
內容是透明的看不見,但碎片的「避開文字」是**照元素位置**算的,
於是整片中央被當成有字挖掉,頭骨中間就破了。
畫面上那幾條淡橫帶就是被挖出來的文字形狀。

修法:只有正在演出的幀才鎖位置。
```js
var live = (vin > 0 && vout < 1);
pin.style.setProperty('--lock', live ? (-pinTop) + 'px' : '0px');
```

**🔴 教訓:透明 ≠ 不存在。** 只要元素還佔著位置,任何「照位置算」的邏輯
(避開文字、IntersectionObserver、命中測試)都還是看得到它。
這個站已經在 clip-path + IntersectionObserver 上踩過一次同類型的坑(見 §18)。

**🔜 下一個要修:Works 列表塞不進一幀**
9 列只顯示得下 4 列,其餘被 `.frame__pin { overflow: hidden }` 裁掉。
跟 About 是同一類問題(§52 ④),但 About 是收間距就夠,
Works 要嘛縮列高、要嘛這一幀改成可以捲兩屏。**還沒處理。**

---

## 52. 龍骨改由捲動驅動 + 開場換掉比例尺(2026-08-01)

**① ⚠️ 「兩頁疊在一起」—— 我上一版改壞的**
我把 p 放開到 >1 讓上一幀「邊滑走邊淡出」,結果直接露出真相:
上一幀的 pin 正往上滑、下一幀的 pin 正從下面上來,**兩張畫面是前後排隊的**,
同時可見就變成兩頁疊在一起,看起來就是壞掉。
修法:算出 pin 偏離螢幕頂端多少,用等量位移把內容推回畫面中央(`--lock`)。
進場和退場的內容都固定在同一個位置,只有透明度在交叉 —— 頁面不動、畫面換一偵。

**② 「顱骨完全沒有散開」+「所有畫面都有龍骨」是同一個病根**
炸開本來綁在「區塊換了沒」+ 固定 900ms 計時器上:
- 跟捲動無關 → 捲慢一點 0.9 秒就播完,人還在首屏上方,往下看時骨頭早沒了
- 區塊判斷會失準 → 骨頭跑到 Works / About 上面

改成直接讀首屏那一幀捲了多少(`hx`),捲動就是播放頭:
```js
hx = (hp - 0.55) / 0.80;          // 首屏捲到 55% 開始散,135% 散完
burst = hx * BURST * min(W,H) * 0.62;   // 一路往外推,不回收
sFade = 1 - hx;                    // 骨頭只活在首屏
```

**③ ⚠️ 骨頭的「位置」也還在用區塊判斷 —— 這是頭骨消失的真兇**
`tgtX = wantSeg === 0 ? W*0.5 : anchorX` —— 幀高改成 200svh 之後 sectionIndex 失準,
首屏時 wantSeg 已經不是 0,骨頭被搬到畫面外、還縮成 0.28 倍。
**載入動畫播完頭骨就不見了,就是這一行。** 骨頭只活在首屏,位置直接固定置中。

**教訓:改透明度之前,先確認元素在不在該在的位置。**
這一輪我先查了 hx(對的)才發現問題在位置,前面白繞了一圈。

**④ 開場動畫換掉比例尺**(Zakk:「開場動畫不要用比例尺」)
比例尺會「自己畫出來」,又是繪圖機那一套 —— 跟剛從過場拿掉的影印感是同一個問題。
改成 ZAKK 四個字母依序從模糊裡對焦出來,跟頁面過場同一套語彙。
JS 只寫一個 `--k`,錯開的計算交給 CSS。

---

## 51. 過場改成粒子交叉淡出(2026-08-01)

Zakk:「過場我想把影印的感覺拿掉,用粒子去做。不太喜歡過場留白的感覺,太空了。
首頁過場龍骨沒有炸開的動畫。每一頁的留白有點太多。」

**① 拿掉「影印」**
舊做法是 `clip-path: inset()` 由下往上掃,再加一條 ember 橫線跟著跑 ——
那個動作就是繪圖機在描,而且線走過之前那塊是全白的。
改成:不裁切,每個直接子元素依 `--d`(排第幾個)錯開自己的進場,
從「模糊 + 下方 30px」浮上來對焦。`--d` 在 JS 建幀時寫進 inline style。
`.frame__pen` 直接 `display: none`。

**② ⚠️ 「過場好空」的真正原因不是動畫時間 —— 是 p 被夾在 [0,1]**
一幀高 200svh(1800px),但 pin 只有 100svh。所以:
```
p  0 ~ 1    釘住不動
p  1 ~ 2    這一幀還在畫面上,只是正往上滑出去
p −1 ~ 0    下一幀正從畫面下緣升上來(內容還在螢幕外)
```
舊寫法夾在 [0,1] → 那 900px 裡上一幀 out 已經是 1(明明還看得到卻被畫成全透明)、
下一幀 in 還是 0 → **整整一個畫面高度是空的**。

我一開始誤判成「淡入太慢」,把 IN_START 往前拉了兩次都沒用,
直到量了 `pin.getBoundingClientRect().top = 864` 才看出內容根本在畫面外。
**教訓:改透明度之前先量元素在不在畫面裡。**

修正後把動畫對齊實際位移:
```js
IN_START = -0.80, IN_END = 0.00, OUT_START = 1.00, OUT_END = 1.80;
if (p < -1.4) p = -1.4; else if (p > 2.2) p = 2.2;   // ⚠️ 上下限都要放開
```
實測:全站最空的一刻從「可見度 0」變成 **0.73**,沒有任何一段是空白的。

**③ 龍骨要在炸開時還看得見**
`sFade` 舊寫法 `1 - scrollY/(H*0.75)` 在 675px 就歸零,
但首屏→Works 的炸開是 900px 才發生 → 骨頭早就不見了。
改成綁 `fe`(過場進度)交叉淡出,炸到一半時骨頭還有一半在。
⚠️ 這裡不能用 `fi`/`toI`,FORMS 只剩一個形體、那兩個永遠是 0,要用 `fromSeg`/`wantSeg`。
非首屏的環繞粒子 0.28 → **0.42**(補留白)。

**④ About 塞進一幀**:947 → 900px。只收間距不動字級,名字 42→36px。

---

## 50. 修 sticky 失效 + About 換照片與自動展開(2026-08-01)

**① ⚠️ 點 Intro 只看得到左下小字,標題不見了 —— 兩層原因疊在一起**

1. 首屏的大字 / 左下說明 / 狀態列都是 `position: absolute`,基準是 `.hero-b`。
   但 `.hero-b` 現在是 **200svh 的外殼**,`top: 64px` 變成「從外殼頂端算」→ 大字飛到畫面上方。
   → `.frame__sheet` 加 `position: relative` 當基準。
2. 改完還是壞的:實測 `top = -426`,pin 根本沒黏住。
   **祖先只要設了 `overflow`(任何方向),子層的 `position: sticky` 就完全失效。**
   `.hero-b` 有**兩處**被設成 `overflow: hidden`(705 行與 1299 行),
   我只改了一處、另一處蓋回來 → 在檔尾直接覆蓋 `.hero-b { overflow: visible; }`。

**修好後**:`top = 64`、在畫面內、無橫向捲動。

**🔴 記住:這個站要用 sticky 的地方,祖先一律不能有 overflow。**
`body` 目前是 `overflow-x: hidden`(Photos 滿版出血用的),
其他幀的 pin 目前仍正常,但這是潛在地雷。

**② About 換成山頂生活照**(Zakk 指定)
`_source/unused/IMG_4446.JPG` → `assets/about/about-summit.webp`(1050×1400,108KB)
⚠️ **手機照片一定要先 `ImageOps.exif_transpose()`** —— 第一版沒套,整張橫躺。
裁切:人物約佔一半高度,切掉大部分天空(原構圖天空太多,縮到 295px 框裡人會太小)。

**③ Experience 自動展開**(Zakk:「不要讓人點了才看到」)
`<details>` 改回一般區塊。為了塞進一幀,兩段職務描述各縮成一行,
並收緊間距(intro 24→16、block 40→24、經歷列 16→10)。

**🔜 還沒做(Zakk 這輪提的,下次接手先做這個)**
- **過場改用粒子,不要「影印/描出來」的感覺**(clip-path 掃描要換掉)
- **首屏過場龍骨要有炸開的動畫**(目前只是淡出)
- 每一幀留白仍偏多,要再加粒子或設計

---

## 49. 幀節奏 / 捲軸 / 照片帶 一次修完(2026-08-01)

**① 播放曲線重配**(Zakk:「轉場太快根本看不出做了什麼,空白停留太長」)
| | 之前 | 現在 |
|---|---|---|
| 描繪 | 0 – 0.28 | **0 – 0.48**(慢一倍,看得出筆在走) |
| 定格 | 0.28 – 0.74(46%) | **0.48 – 0.62(14%)** |
| 退場 | 0.74 – 1 | 0.62 – 1 |

**② 導覽點下去會跳到空白頁**(Zakk 回報)
幀的**最上緣**是「還沒被描出來」的位置,直接跳過去當然是空白。
錨點改成跳到 `frame 起點 + 播放長度 × 0.55`,也就是**已描完的定格區**。

**③ 藏掉捲軸**(Zakk:「把往下拉的條拿掉,很不像在跑影片」)
⚠️ **規則要下在 `html` 不是 `body`** —— 實際在捲的是 documentElement,
第一次下在 body 上,捲軸照樣佔 15px。改對之後量到 **0**。
捲動完全沒被禁用:滾輪、鍵盤、觸控、Page Up/Down 都照常。

**④ 每一幀都留環繞粒子**(Zakk:「每一頁的空白太多」)
骨頭仍然只屬於首屏、捲離就淡掉;但**環繞粒子不再跟著消失**,
離開首屏後降到 28% 而不是歸零(`orbitA` 獨立於 `skullA`)。

**⑤ 照片帶:滾輪會同時捲照片又跳幀**
平滑捲動的 wheel 監聽在 window,照片帶的在元素上(先跑)——
`preventDefault()` 不會阻止冒泡,所以兩邊都執行了。
→ 平滑捲動加上 `if (e.defaultPrevented) return;`。
實測:在照片帶上滾輪 → 照片位移 −440px、**scrollY 完全沒動**。

**⑥ 照片循環接不上**(Zakk:「跑到最後一張沒有輪迴到第一張」)
⚠️ 軌道用 flex `gap`,複製一份後總寬 = 兩份內容 + (2n−1) 個 gap,
但一個完整循環是「一份內容 + **一個** gap」—— **`-50%` 會差一個 gap**,
所以尾端會露出空隙。
→ 改用 JS 實測「第一張 → 複製後第一張」的距離寫進 `--mq-shift`
(實測 6201px),自動捲動與手動滾輪都用同一個值。

**⑦ 載入動畫還在**(Zakk 問的)——
清掉 sessionStorage 後重載,比例尺有跑到 `aria-valuenow=100`。

**實測**:捲軸寬 0、點 Works 落在 clip 全開的定格區、
照片滾輪不跳幀、無 console 錯誤。

---

## 48. 幀轉場:一張正在被描出來的圖紙(2026-08-01)

**⚠️ Zakk 的流程回饋(要記住)**
> 「我幫你裝的東西全部用出來,而不是我要一個個叫出來用,你應該要自己學著主動用」

CLAUDE.md 早就寫了「視覺工作開始前先叫出 frontend-design」,但一直等到被點名才用。
**以後動視覺一律先自己叫技能,不要等對方要求。**

**① 修:首屏一進站字就在**(Zakk:「不要滑了才出來」)
改成 pin 之後,第一幀的播放進度從 0 開始 = 還沒進場 = 透明。
但第一幀前面沒有東西,不該還要「等它被描出來」。
→ `sec.__first` 標記,第一幀的 `--in` 直接固定為 1。

**② 幀轉場的設計:每一幀是一張正在被繪製的圖紙**

之前幀與幀之間是最普通的淡入淡出 —— 而這個站唯一真正屬於 Zakk 的語彙是**圖面**:
載入是比例尺被描出來、換頁是繪圖機掃過、內頁圖面也是繪圖機畫的。
**只有幀轉場斷掉了。**

所以改成:
- **進場**:`clip-path: inset(0 0 (1-in)*100% 0)` —— 內容**由上往下被描出來**
- **筆**:一條 ember 細線騎在描繪邊緣(`top: in*100%`),
  透明度用 `4·t·(1−t)` 的拋物線 → 兩端 0、中途最亮,**畫完不留痕跡**
- **元素錯開是自然發生的**:筆掃到誰誰才出現,不用寫 stagger,
  而且它是真的(筆就是照順序經過)
- **退場**:圖紙往上抽走 + 微縮 + 淡出,比進場快(退場要乾脆)

結構:`.frame > .frame__pin > (.frame__sheet + .frame__pen)`
⚠️ 筆一定要跟 sheet 分層 —— 同一層會被 sheet 的 clip-path 一起裁掉。

**結果:載入、換頁、幀轉場現在是同一個動作 —— 東西正在被畫出來。**

**實測**:第一幀一進站 clip 全開、大字可見;第二幀中途 clip `inset(0 0 55.3%)`、
筆停在 402px(= 44.7% 高度)、ember 色、透明度 0.99;
持續捲動中位數 **16.7ms** / p95 **17.8ms** / 最慢 26ms;無 console 錯誤。

---

## 47. 骨頭只留首屏 + 平滑捲動(2026-08-01)

**① 其他區塊的骨頭全部拿掉**(Zakk:「其他頁的骨頭刪掉,保留首頁的就好」)
- `FORMS` 從五段減成**只有頭骨**。只剩一段之後,換段判斷 / 炸開過場 /
  空白框定位那整套會**自然退化成不作用**(`forms.length - 1 = 0`,
  `sectionIndex` 永遠回傳 0),程式碼留著沒有壞處,要加回去只要把陣列補回來
- 捲離首屏 **完全淡出**(原本留 30%)—— 留一點點只會變成看不出用意的雜訊
- `assets/3d/bones/*.svg` 保留但已無人引用

**② 平滑捲動 —— to-portfolio「順」的真正來源**
實際看過它的原始碼:**它用 Lenis**。那個順不是動畫做得好,
是**捲動位置被內插過**:原生滾輪一格跳 100px,畫面是階梯狀前進;
平滑捲動每幀只走剩餘距離的 9%,所以像影片在播。

自己實作(不裝 Lenis,原理就是一行 lerp):
- **只接管滾輪**。鍵盤、拖捲軸、觸控都走原生 —— 攔了會壞掉無障礙
- `prefers-reduced-motion` 或觸控裝置直接不啟用
- 站內錨點自己接手,不然會跟平滑捲動打架
- 自己做平滑後要把 `scroll-behavior` 設成 `auto`,兩套會互相打架

⚠️ **踩到的坑**:同步條件一開始寫成 `if (!wheeling)`,
結果動畫結束後 `wheeling` 仍是 true 的那段時間裡,
**外部的 `scrollTo` / `scrollIntoView` 完全失效** —— 頁面卡在舊位置。
要用 `running`(動畫是否進行中)才對。

⚠️ 這個 bug 還害我誤判:量到「首屏碎片 = 0」以為頭骨壞了,
其實是 `scrollTo(0,0)` 根本沒生效,量測點還在 About 區(那裡本來就該淡出)。
**量測前要先確認捲動位置真的到了。**

**實測**
| 項目 | 結果 |
|---|---|
| 滾輪 900px 的前 6 幀 | 81 → 155 → 222 → 283 → 339 → 389(逐幀內插) |
| 首屏碎片 | 78,400 像素 |
| 捲離首屏 | **0**(完全乾淨) |
| 回到首屏 | 77,922(正常回來) |
| 導覽錨點 | 正常到位 |
| 幀間隔 | 中位數 **16.7ms** / 最慢 **17.2ms** |
| console | 無錯誤 |

---

## 46. 真正的影片式翻頁:pin + 捲動當播放頭(2026-08-01)

Zakk:「你只是把它從可以慢慢滾變到強制停到下一頁,這並不是影片啊」——**完全正確**。

**⚠️ scroll-snap 不是影片。** 它只是強制停到下一段,頁面本身還是在移動。
真正的機制是 GSAP ScrollTrigger 的 **pin + scrub**:
**把畫面釘住不動,捲動推進的是「這一幀播到哪」**,內容在原地演進出場。

**實作:用原生 `position: sticky`,不裝 GSAP**
本機沒有現成的 GSAP 檔,而 DESIGN.md 的原則是不依賴外部函式庫與伺服器。
sticky 本身就是 pin,進度自己算約 60 行就夠。
(要換成真的 GSAP 也可以,機制一模一樣。)

結構由 JS 在載入時包好,HTML 不用改:
```
<section class="frame">        外殼,高度 200svh = 這一幀的「播放長度」
  <div class="frame__pin">     sticky 釘在視窗上的那一屏
    …原本的內容…
```

**播放曲線**
```
0 ─ 0.28    進場(浮上來、淡入)
0.28 ─ 0.74 定格(完全不動 —— 讀字時畫面必須是靜的)
0.74 ─ 1    退場(往上走、淡出 + 微縮)
```
定格佔了快一半,所以「一幀」感覺得出來是個定格,不是連續捲動。

**實測**(Works 那一幀)
| 播放進度 | pin 在螢幕上的 top | --in | --out | opacity |
|---|---|---|---|---|
| 0 | 108 | 0 | 0 | 0 |
| 0.15 | 20 | 0.553 | 0 | 0.55 |
| **0.5** | **0** ← 完全釘住 | 1 | 0 | 1 |
| 0.9 | -23 | 1 | 0.670 | 0.33 |

中段 pin 的 top = 0,**畫面確實不動,只有內容在演**。

同時移除 `scroll-snap`(它跟 pin 是互斥的兩種做法)。

**實測**:5 幀 5 pin、頁面總高 9175、持續捲動中位數 **16.8ms** / p95 **19.4ms** /
最慢 **22ms**、無 console 錯誤。

**🔜 還可以往下做**:目前是整塊內容一起進出場。
下一步可以讓幀內元素**錯開進場**(標題先、清單後),那會更像分鏡。

---

## 45. 一區 = 一幀(2026-08-01,已被 46 的 pin 取代)

Zakk 選了「保留吸附 + 把每一區壓成一個視窗高」,要**播影片的感覺**。

**① 章節骨架改成「一幀」**
```css
.section {
  min-height: 100svh;
  display: flex; flex-direction: column; justify-content: center;
  padding: var(--s6) var(--gutter);   /* 原本上下各 168px */
}
```
空間改由 `justify-content: center` 給,不再靠巨大內距。
首屏 `min-height` 也從 `calc(100svh - topbar)` 改成 **`100svh`** ——
topbar 是 sticky 疊上去的,不該從高度扣掉,扣了就少 52px、吸附落點對不齊。

**② 各區塞進一幀的手法**
| 區 | 原高 | 手法 | 現高 |
|---|---|---|---|
| 首屏 | 848 | min-height 改 100svh | **900** |
| Works | 1342 | 列內距 24→14、作品名 44→33px | **900** |
| About | 1596 | **Experience 收進 `<details>`**(370px→40)、合併 Tools/Strengths 兩塊、間距微調 | **900** |
| Photos | 959 | 內距收斂 | **900** |
| Contact | 489 | 垂直置中 | **900** |

吸附落點:**52 / 952 / 1852 / 2752 / 3652 —— 等距 900**。

`<details>` 用原生元件不寫 JS:鍵盤、讀螢幕軟體、沒有 JS 的環境都能展開。
展開箭頭自己畫(`+` / `−`),瀏覽器預設的三角形跟這個站的語彙不合。

**③ 吸附改成 mandatory**
每一幀都塞得下之後就不會把人困住。實測:捲動精準停在 900 的倍數;
**展開 Experience 後這一幀變 1233(超出 333),仍然可以正常捲到底**
(實際停在 2185 = 目標值,CTA 看得見,沒有被吸回去)。

**實測**:五區都是 900、無 console 錯誤、幀間隔中位數 **16.7ms**、最慢 **21.8ms**;
Works 幀的脊椎與 About 幀的肋骨都清楚出現在空白帶。

---

## 44. 「下滑沒有炸開」的真正原因是 scroll-snap(2026-07-31)

Zakk 回報下滑時骨頭沒有炸開。查了很久,**炸開機制其實一直是好的**,
用暫時的 `window.__dbg` 出口實測(在真正的吸附落點 848px):
```
idx 0 → 1 ,segT 0 → 1 約 900ms,burst 0 → 402px → 0
```

**真正原因:`scroll-snap-type: y proximity` 把小幅捲動吸回首屏。**
實測:捲到 200px → 停在 **0**;捲到 260px → 停在 **0**;捲到 700px → 跳到 **848**。
所以「往下捲一點點」等於根本沒捲,換段判定當然不會觸發。

⚠️ **debug 的教訓**:一開始一直在調「換段判定線」(0.5 → 0.6 → 0.78),
方向完全錯 —— 因為我沒有先確認**捲動位置本身有沒有生效**。
量測前要先驗證「輸入」是不是我以為的那個值。

**同時做的調整**
- `BURST` 0.34 → **0.95**:0.34 太小,新骨頭同時要縮進小空白框,
  那個「縮」蓋過了「炸」,看起來只是縮小平移
- 換段判定線 0.5 → **0.78**(下一區頂端升過視窗八成高就換段)
- 龍頭在換段前**維持置中不平移** —— 之前加的 `scrollY < H*0.35` 門檻
  會讓它在還沒換段時就慢慢滑走,反而更怪(Zakk:「更慘了」)

**待 Zakk 決定**:proximity 吸附造成「捲一點點會被拉回去」。
這其實符合他要的「一幀一幀」,但手感上會覺得在跟頁面拔河。
要嘛保留(配合影片式切換),要嘛拿掉吸附讓捲動自由。

---

## 43. ⚠️ main.js 被編輯腳本炸成 76MB(2026-07-31,**已修復**)

**事故**:`js/main.js` 從 48KB 變成 **76,172,091 字元**,語法錯誤 →
**整支 JS 完全沒有執行**:所有 `.reveal` 內容維持透明(18 個一個都沒觸發)、
碎片沒畫、preloader 卡住。畫面近乎全白。

**原因**:編輯腳本用了
```python
old = s[s.index("A") : s.index("B")]      # B 在 A 前面 → 切出空字串
s = s.replace(old, new)                   # replace('', x) 會把 x 插進每個字元之間
```
`s.index("B")` 找到的位置在 `A` **之前**(FEATHER 在 588 行、標記 B 在 590 行、
標記 A 在 668 行),切片變成 `''`,而 Python 的 `str.replace('', x)`
會在**每一個字元之間**插入 x → 48KB × 1536 ≈ 74MB。

**修復**:插入的是固定區塊 B,所以檔案是 `B + c0 + B + c1 + ...`。
用「前 400 字當探針、找它第二次出現的位置」推出 `len(B)`,再全域移除 B → 完整還原。

**🔴 鐵則(以後一定要遵守)**
1. **絕對不要用 `s.index()` 切片來組 `old`** —— 順序假設一旦錯就切出空字串
2. 每個替換前必須 `assert old` 且 `assert s.count(old) == 1`
3. 改完 JS **一定要驗語法**:
   `fetch('/js/main.js').then(r=>r.text()).then(t=>{ new Function(t) })`
   —— 這次如果早點驗,就不會拿著沒執行的 JS 量出「60 FPS」的假數據
4. ⚠️ **量到的效能數字要合理懷疑**:JS 沒跑時 rAF 照樣 60fps,
   幀時間也漂亮 —— 那不代表引擎在運作

**同時完成的改動(重做後已驗證)**
- **遮蔽改查表**:原本每顆碎片對所有文字框算距離(3700 × 40 ≈ 15 萬次/幀),
  改成每 20 幀算一張 48×32 遮蔽表 + 雙線性取樣 → 每顆 O(1)。
  (Zakk:「滑鼠扭曲有點掉幀的感覺」)
- 滑鼠:半徑 88 → **62**、切線 46 → **34**、外推 26 → **16**、
  游標追蹤 0.12 → **0.22**(太黏會讓人覺得在掉幀)
- **龍頭捲離首屏就不再強制置中**(`scrollY < H*0.35` 才置中)——
  之前會卡在畫面正中壓住 Works 標題

**實測(這次 JS 確實有執行:`__loadK = 1`、碎片 78730 有色像素)**
滑鼠持續繞圈中:中位數 **16.7ms**、p95 **18.3ms**、最慢 **21.6ms**;
18 個 reveal 捲完 17 個觸發(剩 1 個是隱藏的 A 版 hero);無 console 錯誤。

---

## 42. 修:首屏畫成脊椎不是頭骨(2026-07-31)

Zakk 回報「首頁頭骨變成這樣??」——首屏顯示的是一條弧形線稿,不是頭骨。

**⚠️ 原因:`offsetTop` 不是文件座標**
`sectionIndex()` 用 `el.offsetTop` 判斷區塊位置,但 `offsetTop` 是相對
**最近的定位祖先**算的。`#main` 有 `position: relative`(style.css 那條
`.hero-b, #main, .footer { position: relative; z-index: 1 }`),
所以 `#works.offsetTop` 是 **0** 而不是 900。

結果首屏 `mid = 450 >= 0` → 判定成第 2 段 → 畫脊椎。

**修法**:改用 `el.getBoundingClientRect().top + scrollY`(真正的文件座標)。
這個查詢會逼瀏覽器重算版面,所以跟 `measureAvoid()` 一起、每 20 幀才做一次
(`measureSegs()`)。

**教訓**:這個站有多處 `position: relative` 的容器,
**要文件座標一律用 `getBoundingClientRect().top + scrollY`,不要用 `offsetTop`。**

實測:首屏頭骨恢復,而且比之前更實(NPTS 3600 + 濃度倍率 1.45)、無 console 錯誤。

---

## 41. 骨頭只在換區塊時動 + 捲動吸附(2026-07-31)

Zakk:「同一頁閱讀時粒子還跑動會影響閱讀,粒子到下一頁就待在那個地方」
「切換頁都像播影片的方式,像案例一樣」

**① 骨頭改成「換區塊才播一次過場」**
- 之前是把捲動位置**連續**映射到骨頭段落 → 只要在捲,骨頭就一直在變形
- 改成 `sectionIndex()` 判斷視窗中央落在哪一區,**只有區塊換了**才播一次
  固定 900ms 的過場(炸開 + 重組),播完停住
- 停住時骨頭的晃動收到 **0.12**(幾乎不動);環繞粒子不受影響

**⚠️ 改壞過一次:落點存成「文件座標」→ 頭骨不見了**
想讓骨頭「待在那個地方」,一度改成記文件座標、跟著頁面捲 ——
結果捲過去之後骨頭直接離開畫面。Zakk 回報「頭骨怎麼不見了」。
**要的是「停在那裡不再游移」,不是「跟著頁面捲走」。**
正解:記**畫面座標**,而且只在過場過半(`segT > 0.55`,新一區已進畫面)量一次就鎖住。

**② 首屏頭骨更明顯**(Zakk:「首頁的龍骨再明顯一點」,位置維持不動)
- 新增每段的濃度倍率 `am`:**頭骨 1.45**,其餘 1.00
- 頭骨取樣 `iw/190` → **`iw/230`**;NPTS 3000 → **3600**
- 首屏仍強制置中、縮放 1.0,位置沒動

**③ 捲動吸附(`scroll-snap-type: y proximity`)**
每一區塊 `scroll-snap-align: start`,捲到附近會「卡」上去,像一頁一頁定格。

⚠️ **用 proximity 不是 mandatory**:實測 5 個區塊裡有 **3 個比視窗高**
(1342 / 1596 / 959 vs 視窗 900)。mandatory 會在使用者讀到一半時硬拉走,
長內容根本讀不完。

**⚠️ 沒辦法做到案例那種「畫面完全固定、一幀一幀跑」——**
那些站(to-portfolio)全站只有約十個字,所以可以鎖死視窗。
這個站有九件作品、完整履歷、照片帶,鎖死視窗會直接把內容切掉看不完。
要真的做到,得先把每一區的內容壓到一個視窗高以內 —— 那是內容層級的改動,不是加個 CSS。

**實測**:無 console 錯誤;首屏頭骨 25588 有色像素(回來了)。

---

## 40. 骨頭塞進「最大的一塊空白」+ 滑鼠改纏繞(2026-07-31)

Zakk:「骨頭在空白處明顯可見這部分完全沒有,根本看不出來是骨頭,空白處的粒子太大片」

**⚠️ 上一版錯在哪**:取的是**所有空白格的重心**。但空白通常散在上緣、左欄、右側,
重心落在中間 → 骨頭被攤成一大片、密度極低 → 完全讀不出形狀。
**空白的「重心」不等於「可以放東西的地方」。**

**改成:找最大的一塊連續空白矩形,把骨頭整個塞進去**
- 18×12 網格標記空白格 → **直方圖 + 單調堆疊**找最大的全空矩形(O(GX·GY))
- 每一段骨頭在 build 時記下自己的實際寬高(`formExt`)
- 縮放取 `min(框寬×0.92/骨寬, 框高×0.92/骨高)`,夾在 0.28–1.0
- 小而密 → 看得出是骨頭。實測 Contact 區的尾椎清楚可辨

**其餘照 Zakk 指示**
- 環繞粒子 **460 → 130**(「太雜了」)
- 環繞動態改成**慢而穩的近正圓**:速度 0.5–2.0 → **0.16–0.36**,ecc 0.4–1.3 → **0.75–1.10**。
  之前各轉各的、又快又扁,整團就是雜訊
- **滑鼠改成「纏繞」**:原本只有往外推,現在以**切線位移為主**(SWIRL 46 : PUSH 26),
  碎片會繞著游標轉而不是被彈開;半徑 120 → **88**

**文字密集區(如 Works)**:最大空白框很小 → 骨頭縮到下限、幾乎隱形,
只剩少量環繞粒子。這是 Zakk 要的(「其他地方可以用少少的粒子環繞就好」)。

**實測**:首屏連續兩次 60 FPS、無 console 錯誤;Contact 尾椎、首屏頭骨都清楚。

---

## 39. 骨頭自己去找空白處(2026-07-31)

Zakk 回報 About 區「幾乎看不到骨頭的形狀…連粒子都不見了」。
原因是三件事疊在一起:整層降到 30%、避讓羽化、骨頭正好落在文字欄後面。

**解法:不要用猜的去排每個區塊,讓骨頭自己找空白。**
- `measureAvoid()` 每 20 幀順便掃一張 **18×12 的粗網格**,
  格子中心離所有文字框都超過羽化距離 = 空白
- 取所有空白格的**重心**當骨頭的落點,並依空白比例縮放(`emptyK`,不小於 0.5)
- 平滑追蹤(係數 0.05),換區塊時不會瞬間跳過去
- 首屏維持置中不偏移(那裡本來就空)
- 環繞粒子不受影響,仍然滿版繞

**整層濃度 30% → 75%** —— 之前為了不擋字把整層調暗,結果連粒子都看不見。
真正防止擋字的是避讓,不是把整層調暗,這兩件事不該混在一起做。

**⚠️ 效能量測的陷阱(又一個)**
第一次量到首屏只有 **4 FPS**,以為引擎爆了。實際上是量測當下正在跑
「從頁尾平滑捲回頁首」的長距離動畫。停穩後連續三次都是 **60 FPS**,
另外用 `scroll-behavior: auto` 手動連續捲動實測也是 **60 FPS**。
→ **量 FPS 前要先確認捲動已經停止**(跟 §25 記過的 `document.hidden` 是同一類陷阱)。

**實測**:靜止 60 FPS、持續捲動中 60 FPS、無 console 錯誤;
About 與 Photos 截圖確認碎片明顯出現在空白處,文字側乾淨。

---

## 38. 其他段骨頭:小而密(2026-07-31)

Zakk:「其他頁骨頭可以小但多一點,形狀可以再更明顯一點」

- **每一段有自己的顆粒倍率**(`FORMS[].fs` / `.wob`),過場時跟著插值:
  頭骨 `fs 1.00 / wob 1.00`(實心塊面,要份量);
  線稿骨頭 `fs 0.56–0.60 / wob 0.45–0.50` —— 點都擠在細線上,
  **顆粒一大就糊成一條粗帶**,小才會利
- NPTS 2200 → **3000**;線稿取樣 `iw/380` → **`iw/520`**(更密)
- 環繞粒子不受影響(`p.idx < 0` 時倍率固定 1),維持原本的動感

**⚠️ 避讓框要抓「有字的小塊」,不能抓容器**
原本選擇器有 `main li` —— Works 每一列都是整行寬,避讓框直接吃掉整個畫面,
碎片全被擋光、形狀反而完全看不見。改抓欄位層級
(`.works__num / .works__name / .works__type / .works__year` 等),
欄與欄之間的空隙才留得下來。羽化也從 90 → **56px**。

**⚠️ 一個沒解的取捨(要 Zakk 決定)**
「碎片不要在字周圍」和「形狀要更明顯」在**文字密集的區塊會直接衝突**。
實測:Works 是整片列表,即使抓到欄位層級,骨頭仍然幾乎看不見。
目前的行為是:
- **首屏、區塊之間的空白帶、About 右側** → 碎片清楚可見(過場的炸開就發生在這些空白帶)
- **Works 這種整片文字** → 骨頭幾乎隱形

可能的下一步:讓每一段骨頭有自己的位置偏移(往該區塊的空白處靠),
而不是всё都置中;或接受文字密集區就是讓位。

**實測**:3000 點仍然 **60 FPS**、無 console 錯誤。

---

## 37. 沿骨架走 + 文字避讓(2026-07-29)

**① 五段骨頭,捲動一路重組**(Zakk:「下滑時碎片炸開,合併成下一頁的骨頭形體」)

`assets/3d/bones/` 新增 **ribs.svg / wing.svg / tail.svg**(都是程式生成的向量,不是網路圖),
加上原有的頭骨與脊椎,共五段對應五個區塊:

```
頭骨 → 脊椎 → 肋骨 → 翅膀 → 尾椎
```

- 每一段都取樣成**同樣的 NPTS = 2200 點**(`fit()` 多的抽樣、少的重複加抖動)——
  數量一樣才能逐點插值
- 捲動位置映射到 `0 → 段數-1`:整數 = 停在某一段骨頭上,中間 = 一邊插值一邊炸開
- **炸開**:`burst = sin(t·π) × BURST`,每顆碎片有自己的 3D 方向 →
  過場中間會散開,但兩端都是看得出來的形,**不會變成雜訊**(這是上次走廊版失敗的主因)
- ⚠️ NPTS 一開始設 900,頭骨完全讀不出來 → 2200 才夠

**② 文字避讓**(Zakk:「碎片不要在字周圍,排版在空白處就好,很難閱讀」)

⚠️ 照 PROGRESS §18 的教訓做:**用「推開」會在框邊堆成一條生硬直線,要用淡出**。

- 每 20 幀量一次畫面內所有文字元素的框(每幀量會逼瀏覽器重算版面,很貴)
- 框內**完全不畫**,框外 90px 羽化淡出
- 離開首屏後整層再降到 30% 濃度(首屏沒什麼字可以放滿,下面全是要讀的)

**③ 其他微調**(Zakk 指示)
- 宣言 clamp(2.6–5.6rem) → **clamp(2.1–4.2rem)**(1440px 下 89.6 → 72px)
- 骨頭碎片 gap×0.30–0.72 → **0.30–0.70**、NPTS 提高後密度自然回來
- 環繞粒子幅度 10–36 → **26–80**、速度 0.2–0.9 → **0.5–2.0**(「背景粒子沒有動感」)
- 炸開幅度 0.55 → 0.34,外圈半徑收小

**實測**:首屏與內容區都 **60 FPS**(避讓的逐顆距離計算沒有拖慢)、無 console 錯誤;
About 區實際截圖確認文字周圍乾淨,碎片只在上緣與左側空白處。

---

## 36. 首屏重構:左上宣言 + 碎片回不規則(2026-07-29)

Zakk 授權自行判斷(「都先自動經過同意…盡量做好看,不一定要全聽我的」)。

**① 碎片(照 Zakk 指示改回來)**
- 方塊 / 空心方塊 **不用了**,改回**不規則多邊形**
- **輪廓圈移除**(參考 ricardochance 加的那圈邊緣點,Zakk 不要)
- 環繞粒子 150 → **460**,半徑 0.85–2.35R、飄動幅度 10–36 → 散到畫面邊緣,補「畫面感」
- 滑鼠扭曲範圍縮小:半徑 190 → **120px**,推力 62 → **54**
- ⚠️ 顆粒形狀由編號決定、不用亂數 —— 透明度烤進 sprite,淡出時 band 會變,
  亂數會讓同一顆碎片在淡出過程中換形狀

**② 首屏文字重構(這是我判斷影響「好看」最大的一項)**

問題(前面診斷過兩次都沒解):
- 首屏最大的字 `Portfolio` 只有 34px,**比下面 Works 清單的作品名 44px 還小**
  → 開場沒有主張,個性全靠粒子撐,而粒子撐不起來
- `Portfolio` 是 Archivo、左下說明是 JetBrains Mono → 同一屏兩套字互不相干
- 而且 `Portfolio` 這個詞**沒有資訊量**

改法(參考 ricardochance 的構圖:小標籤 + 大宣言,靠左上):
```
左上   PRODUCT · SPATIAL · UX / UI · CREATIVE   ← 等寬小標籤
       From space                                ← 宣言,Archivo
       to screen.                                ← 第二行退成石灰
中間   頭骨碎片
左下   Zakk Tseng 曾莛翔 / 身分說明               ← HUD 小字
```
- **宣言換成 `From space to screen.`** —— 那是 DESIGN.md 裡的定位句,
  原本被埋在左下角 10.5px,主次是反的
- 字體不是「兩套打架」而是**分工**:等寬管標籤與 HUD,Archivo 只給這一句
- 第二行退成 `--stone`:一句話裡有主有次,眼睛才有落點
- 位置從「左邊垂直置中」改到**左上**(Zakk 提的)——
  左欄變成上下框,中間整片讓給頭骨;原本置中會跟碎片擠在同一條水平線

**實測**:1440×900 宣言 89.6px、區塊寬 517px、與左下說明留白 441px、無重疊;
616px 窄視窗宣言 41.6px、無重疊、無橫向捲動;兩種尺寸都沒有 console 錯誤。

**還沒做**:「畫面感」仍偏空(環繞粒子只有約 159 顆進到畫面且都很小)。
下一步可走:再加量 / 外圍放大當前景 / 加第二層更遠更淡的做前後層次。

---

## 35. 碎片改成「材質圖例」方塊 + 密度砍 75%(2026-07-29)

Zakk:「沒有很好看,是不是碎片太多,看起來很雜,感覺要變得像粒子一點」——正確。

**問題不是大小,是密度。** 原本 3115 顆、間距 4px、直徑 3–9px,
顆粒互相黏在一起 → 讀起來是一片雜訊材質,不是粒子。
**要讀得出「粒子」,眼睛必須分辨得出單獨的個體**,所以間距要大於顆粒本身。

用 `_dev/preview_fragments.py` 實跑四組密度後定案:

| | 顆數 | 讀起來 |
|---|---|---|
| A 原本 | 3115 | 雜訊材質 |
| B | 1392 | 開始看得出個體,仍忙 |
| **C(採用)** | **778** | **粒子感出來,頭骨的形也還在** |
| D | 349 | 最像粒子,但形散掉了 |

**顆粒造型改成方塊(實心 + 空心)** —— 這不是「比較好看」,是**從 Zakk 自己的東西來的**:
他平面圖左下角那排材質圖例(柏油路面 / 淺色白磚 / 草坪造景 / 磨石子地板)
就是一格一格的小方塊,有的實心有的只有外框。
碎片用同一套語彙,首屏才跟內頁的圖面是同一個視覺家族。
原本的隨機多邊形放大後全是同方向三角形,讀起來只是「碎屑」。

- 環繞粒子 420 → **150**(主體只剩 778,環繞太多會蓋過骨頭)
- ⚠️ **方塊尺寸由編號決定、不能用亂數** —— 透明度是烤進 sprite 的,
  碎片淡出時 band 會變,亂數會讓同一顆碎片在淡出過程中換形狀

⚠️ **又踩了一次快取坑**:改完 `main.js` 忘記把 `?v=` 加一,
Zakk 看到的還是舊檔,回報「看起來一樣」。PROGRESS §18 早就記過這個坑。
→ 做了 `_dev/bump.py`,`python _dev/bump.py` 一次更新所有頁面的版本號。

---

## 34. 解決「看不到自己改的東西」+ 碎片放大(2026-07-29)

**問題**:對話裡的預覽窗格常常沒顯示。窗格隱藏時瀏覽器不合成畫面 → 截圖失敗;
`requestAnimationFrame` 被節流到約 1fps → canvas 幾乎不重畫,連 `getImageData`
讀到的都是好幾秒前的舊畫面。結果就是「改了卻看不到,只能用猜的」。

**解法**:`_dev/preview_fragments.py` —— 把引擎的數學在 Python 重跑一次輸出 PNG。
不依賴瀏覽器,隨時看得到。

⚠️ **踩過的坑**:第一版模擬器用**實心圓**畫,墨量高估很多,
得出「碎片糊成一團」的**錯誤結論**,還照這個結論去否定自己的改動。
真實 sprite 是 16px 畫布裡的 3~5 邊隨機多邊形,墨量少得多。
**改 main.js 的 makeFrags / build / render 時,模擬器要跟著同步**,不然結論會反過來。

**碎片調整**(Zakk:「碎片有點太小,可以像之前偏粒子感,有自己一點的軌跡去動,但又能維持形狀」)
| | 之前 | 現在 |
|---|---|---|
| 直徑 | 3.5–8.7px | **6.5–19.2px** |
| 軌跡幅度 | 2–8 | **4–17** |
| 軌跡形狀 | 正弦飄動 | **每顆自己的閉合橢圓** + 一層慢速擺動 |

- **顆數沒變**(3115),只有大小改了
- 關鍵是**閉合橢圓**(`cos/sin` 用同一個角度)—— 繞一圈回到原位,
  所以有軌跡感但形狀不散。線性飄移不行:會愈飄愈遠、形就糊了
- 實測結果跟直覺相反:**顆粒放大後頭骨的形反而更清楚**
  (看得出角、眼窩、上顎、下顎)

---

## 33. 走廊退回 + 確認參考站的真實作法(2026-07-29)

**⚠️ 最大的教訓:我從來沒打開過參考網站。**
Zakk 給過 `to-portfolio.com` 的網址(還有 iancoad / roshan-sahu / aboutluca 等),
我一直只讀 PROGRESS 裡的文字描述在做設計。這是所有偏差的根源。
**以後拿到參考站,先開來看,不要憑截圖或描述猜。**

**實際打開 to-portfolio.com 之後量到的:**
| | to-portfolio | 這個站 |
|---|---|---|
| 引擎 | **WebGL2**,真 3D 幾何 + 真的霧 | Canvas 2D 貼圖,平面上假裝深度 |
| 捲動 | **沒有捲軸**(body 高 = 視窗高 1288),滾輪被 Lenis 攔截去推鏡頭 | 真的在捲頁面 |
| 內容 | **全站約十個字**的 HUD 文字 | 作品內頁、圖面、履歷、照片 |

第三點是關鍵:**它能讓效果鋪滿畫面,因為它沒有東西要讀。**
這個站有長文與圖面,滿版動態放在可讀文字後面永遠會打架。

**Zakk 的評語(完全正確):**
> 碎片只是炸開來,結果還擋到一堆字…根本不是動畫
> 碎片也沒有邏輯,只是炸開,部分骨頭的設計也不見了

散開到隨機管子之後**不再是任何形狀**,骨頭只在捲動 0 存在。
為了追一個通用的「粒子朝鏡頭飛」,把唯一好的東西(看得出形狀的骨架)拆掉了。

**已退回**:走廊 / 霧化走廊 / 隨機管座標 / disp 全部移除,殘留 0。
碎片回到「只在首屏成形,捲離時往下拖、散開、淡出」。
canvas 保留 fixed 滿版(下一步需要)。透明度烤進 sprite 的作法保留(那是對的)。

**方向已定(Zakk 選)**:不做 3D,把骨架做成**有邏輯的形**。

---

## 32. 碎片走廊 + 每頁不同骨頭(2026-07-29,**已退回,見 33**)

**① 捲動 = 鏡頭往前推(深度走廊)**
- 碎片 canvas 從 hero 裡搬出來(`.hb2-particles` → `.fragments`),改 **fixed 滿版**,
  不然捲走就沒了
- 每顆碎片有**兩個身分**:頭骨上的位置 + 走廊裡的位置,由捲動位置插值:
  `disp` 0 = 頭骨(首屏)→ 1 = 走廊。一個視窗高之內散完
- 走廊用 `sc = F / z`(不是 `F / (F - z)`)才有真正的縱深:近的大、遠的小。
  z 用 modulo 循環 → 走廊無限長
- ⚠️ **踩到的坑**:用 Python `str.replace()` 刪微塵時,兩個 IIFE 的 `layout()`
  程式碼一模一樣,把首屏頭骨的 `build()` 也刪了 → 兩張 canvas 全白,
  **且 console 沒有任何錯誤**,是數像素才發現的

**② 每一頁不同骨頭部位**(Zakk 的想法)
- canvas 上用 `data-src`(哪張圖)+ `data-crop`(取哪一塊,0–1 比例)
  + `data-flat`(向量線稿)決定
- ⚠️ **版權**:Zakk 想用網路上找的龍骨圖。已說明:裁切一部分不改變性質,
  一樣是重製,翻拍書頁尤其明顯。改用**自己生成的向量骨頭**
  (`assets/3d/bones/*.svg`,程式產生),取樣比翻拍照片乾淨得多,
  而且跟全站的技術圖面語言一致
- `data-flat`:線稿是純黑、明暗全一樣,不能用亮度當立體起伏(會變紙片),
  改用亂數給厚度
- ⚠️ 線稿的取樣間距要比照片密(`cw/420` vs `cw/200`)——
  骨頭的線只有幾 px 寬,用照片的間距會整條線被跳過,變散點。
  離線驗算:step 3 + 線寬 7 → 約 **6642 顆**,涵蓋整條脊椎

**③ 依 design-dna 分析參考圖後改的**(Zakk 指出技能沒被用到,補做)
參考圖(to-portfolio 類)有四件事這個網站沒有:
| 參考圖 | 原本 |
|---|---|
| **霧化**:遠的溶進背景、失去對比 | 遠近一樣濃 |
| 近的是**大實體** | 全是小碎屑 |
| **中間是空的** | 中間一團擋住字 |
| 全 `[HOME]` 方括號等寬字 | 混了兩套字 |

- **霧化**:`fog = (1-zn)²`,最近一段也拉回來避免大色斑。
  ⚠️ 每顆碎片不同透明度不能用 `globalAlpha` —— 每改一次就是一次狀態切換,
  上萬顆會掉幀。改成**把透明度烤進 sprite**(`fragsFor(col, band)`,band 1–20),
  整幀只設一次 `globalAlpha = 1`
- **中間淨空**:走廊半徑下限拉到 170(原本 40)—— 這同時解掉 Zakk 說的
  「擋在字後面很難閱讀」
- 第四點(字體)還沒做

**④ 刪掉 Contact 的骨架裝飾**(Zakk:「很醜」)—— HTML 與 CSS 都已清乾淨

**還沒驗證**:預覽窗格是隱藏狀態,rAF 被節流、也截不了圖,
所以 **FPS 與實際視覺都還沒量**。已確認的只有:碎片有在畫(取樣區 10731 有色像素、
透明度有分層)、無 console 錯誤、骨架殘留 0。

---

## 🔜 四個提案(2026-07-29,①② 已開工,③④ 未做)

Zakk 給了參考截圖(to-portfolio.com 那類:灰階、方塊有遠有近、霧化、HUD 等寬小字),
說「**還是沒有他這個感覺**」,並指出三件事:

> 他的每一頁都可以用滾輪去帶動畫面,且有動感的去接續到下一頁
> 首頁的字像 Portfolio 也可以有動畫;字體也跟左下角一樣,現在很像兩個很不一樣的字體
> 首頁的頭骨感覺不夠好看,但我想法有點枯竭了

**診斷:參考圖的核心不是「粒子」,是「深度」。**
現在的碎片是「貼在畫面上飄」,不是「在空間裡被穿過」。差別就在這。

### ① 滾輪 = 鏡頭往前推(關鍵)
把碎片放進真實 Z 軸,`scrollY` 對應鏡頭前進距離。
近的大而實、掠過畫面邊緣;遠的小而淡、慢慢逼近(空氣透視)。
區塊之間鏡頭不停 → 「接續到下一頁」是連續的。
作法:把現有的 `sc = 900 / (900 - z)` 從「區塊碎片雲」擴大成整頁一條走廊。

### ② 頭骨:別讓它一直待在那裡(我的建議)
問題不是不好看,是它「停在畫面中央」沒有任務。改成:
`載入 → 碎片聚成頭骨` → `開始捲動 → 頭骨炸開成往身後流過去的走廊` →
`各區塊 → 從走廊撈出自己的碎片雲`。
頭骨變成整段旅程的起點,而且跟載入動畫接得上;碎片從頭到尾是同一批,只是換隊形。
①②本來就是同一件事,要做就一起做。

### ③ 首屏字體不對應(Zakk 指出的)
`Portfolio` 是 Archivo、左下角是 JetBrains Mono,兩套字放同一屏又都不大 →
看起來像兩個不相干的東西。參考圖裡**全部都是等寬字**,技術介面感就是這樣來的。
選項:(a) 改等寬 + 放大到比 Works 清單大 (b) 換成名字或定位句 (c) 維持 Archivo 只放大。

### ④ Portfolio 的進場動畫
接著載入做:**畫完比例尺的那支筆,接著把字寫出來**(逐字母)。
載入與首頁就變成同一個連續動作,不是兩段。

**其他待辦**
- Works 第 03 項名稱可能是錯的(`Commercial Space / Sugar Reduction` vs 實為 VEGIC 即期蔬果)
- 其餘 8 件內頁(素材與圖說都已在 `assets/works/works-images.json`)

### 30. 用技能做評量 + 修客觀缺陷(2026-07-28)

用 `ui-ux-pro-max` 與 `motion-design` 兩個技能的框架,對整站做量測型評量。

**評量通過(不用動)**
- 效能 **60 FPS**(兩張 canvas 同時跑);33 張圖全部 WebP + lazy + 有 width/height + 有 alt
  → CLS 防護滿分
- 語意化 token、`prefers-reduced-motion`、`focus-visible`、skip-link、hash 深層連結都有
- 風格定位正確:資料庫對「設計作品集 + 編輯極簡」推薦的就是
  **Swiss Modernism 2.0 + Archivo**,跟既有選擇一致

**已修的三個缺陷(A 階段)**
1. **`--stone` 對比 4.29:1 沒過 WCAG AA** → 改 `#646E75`(**4.81:1**)。
   DESIGN.md 原本寫 4.6 是錯的,已更正。順帶實測 `--slate` 4.57、`--ember` 4.53,
   都是低空飛過,**不要再調亮**
2. **手機導覽觸控區只有 16×23px**(標準 44×44)→ 用 `@media (pointer: coarse)` +
   看不見的 `::before` 擴張塊補到 44×44。
   ⚠️ 不能用 padding —— 導覽底線是 `bottom: 0` 定位的,padding 會把線推離文字。
   間距要 38px 不是 36px:「01」實際寬 15.6px,36px 量出來中心距只有 51.6,差一點點
3. **Works 九行長得一樣但只有兩行能點** → 只有 `a.works__line` 才有位移與橘編號;
   有連結的行名字後面帶小箭頭:**→ 站內作品頁 / ↗ 另開分頁**,沒箭頭就是還沒有頁面

**B 階段:動態系統化(已完成)**

動態性格定為 **Premium**(優雅、克制、無 overshoot),跟編輯極簡的底一致。

1. **時間調色盤:30 個散落的值 → 4 個 token**

   | token | 值 | 用途 |
   |---|---|---|
   | `--t-quick` | 160ms | hover、變色、按壓 |
   | `--t-base` | 320ms | 狀態切換、預覽圖跟隨、線條展開 |
   | `--t-slow` | 620ms | 進場、換頁、遮罩升起 |
   | `--t-draw` | 1100ms | 描線(骨架 SVG、圖面的繪圖機掃描) |

   `--t-draw` 是**不同類別**不是第四檔:motion-design 把「戲劇性揭示」列為 600–1200ms,
   本來就該比介面慢。共替換 22 處,`--t-fast` 舊名已完全移除。
   **錯開延遲(stagger)沒有動** —— 那本來就該各自不同。

2. **進場減速、退場加速**:新增 `--ease-exit: cubic-bezier(0.4, 0, 1, 1)`。
   之前不分進退都用同一條 ease-out,消失時會拖泥帶水。目前用在換頁的舊頁退場。

3. **閱讀區安靜下來**(motion-design 的 1/3 法則,Zakk 選「只收次要層」)
   - `calmAt(scrollY)`:首屏維持 1,離開首屏平滑降到 **0.45**(smoothstep,不會有界線)
   - 吃 calm 的:基礎碎片的**亮度、飄動幅度、捲動拖曳**
   - **不吃 calm 的:區塊碎片雲的亮度** —— 那是每一區的招牌,調暗等於把設計拿掉。
     只收它的捲動拖曳(收「動」不收「亮」)
   - 實測(在內頁量,內頁沒有碎片雲所以量到的是純基礎層):
     **100% → 69.8% → 46.3% → 45.3%**,正好命中 0.45

**實測**:60 FPS、無 console 錯誤、無橫向捲動。

**🔜 還沒做:C 視覺再提升** —— 主頁節奏、Works 清單、About / Photos 的版面張力

### 29. 換頁轉場:名字飛過去變成大標(2026-07-28)

主頁與內頁是不同 HTML 檔,用**瀏覽器原生的 View Transitions**(跨文件轉場)。
不支援的瀏覽器就是直接換頁,不會壞、不會卡,所以沒寫備案。

- **CSS 在 `style.css` 尾段**(兩邊都載得到,轉場才會啟動):
  `@view-transition { navigation: auto; }` + 整頁交叉淡出淡入(舊 200ms / 新 300ms 延遲 60ms)
- **主角效果**:點 Works 某一行 → **那行字飛過去、變成內頁的 96px 大標**。
  `view-transition-name: work-title` 兩端對上就會 morph
- ⚠️ **`view-transition-name` 不能寫死在 Works 的九行上** —— 同一頁不能有兩個同名,
  會互相衝突。改由 **JS 在點擊當下**才把 `.vt-title` 掛到被點的那一行(main.js 最後一段)
- **回程**:離開內頁時把大標的名字設成 `none`,否則它會在舊頁快照上飛向一個
  不存在的位置。回主頁就單純交叉淡出
- Works 第 03 項改成 `<a href="works/vegic/">`(原本是 `<div>`,沒有連結)
- 內頁返回連結改成 `../../#works`(原本 `../../index.html#works`,
  會讓主頁有 `/` 和 `/index.html` 兩個網址)

**順手修掉一個會毀掉轉場感受的問題:**
- **從內頁按返回,主頁會整個重播一次載入動畫**(約 2 秒)。圖都在快取裡了還讓人等,
  換頁的連續感就沒了
- 修法:`sessionStorage` 記 `zakk-intro-played`,**同一次瀏覽只播一次**。
  跳過時要補 `window.__loadK = 1`,頭骨才會直接是組好的狀態、不從碎片重來
- 關掉分頁就重來,所以真正的第一次訪客還是看得到開場

**實測**:兩端 `view-transition-name` 都對上、只有被點那行掛到標記、
回程名稱正確清成 `none`、返回主頁 preloader `display:none` 且 `__loadK=1`、無 console 錯誤。

**🔜 還沒做**
- 其餘 8 件內頁還沒做,所以 Works 只有第 03 項能點進去
- **Works 第 03 項的名稱可能是錯的**:現在叫 `Commercial Space / Sugar Reduction`,
  但這件專案是 **VEGIC 即期蔬果(惜食)**,跟「減糖」無關 —— PDF 裡也沒有減糖專案。
  等 Zakk 確認要不要改成 `VEGIC`(改了轉場也更連貫:清單那行字直接變成大標)

### 28. 作品內頁:素材管線 + VEGIC 內頁模板(2026-07-28)

**① PDF 對照解決了** —— 見 `PORTFOLIO-PDF-MAP.md`
- 來源**改用 `_source/portfolio/作品集 NEW2 (1).ai`**(fitz 可以直接開)。
  PDF 版的圖被壓過(渲染圖只剩 300–500px);.ai 是原始檔(渲染 1920×1080、
  平面圖 3509×2482),而且是**比較新的版本**(43 頁 vs 40 頁)
- `_source/portfolio/extracted/` 那 134 張**作廢**:混了兩套、頁碼對不起來、去重誤刪

**② 三支工具**
- `_dev/extract_pdf.py` — 從 .ai 抽圖 + 抓「┌ xxx」圖說 → `_dev/pdf-extract/`,
  並產生挑圖頁 `_dev/pick-images.html`(勾選 → 複製清單)
- `_dev/build_work_images.py` — 把挑好的轉成 WebP 進 `assets/works/<slug>/`,
  同時寫 `assets/works/works-images.json`(含圖說、圖面/渲染分類、浮水印標記)
- Zakk 已挑 **91 張**,轉出後 **6.67 MB**(大圖長邊 1600 + 縮圖 640)

**③ 內頁的設計主張:「圖面直接印在紙上」**
- **開場不用漂亮渲染,用一樓平面配置圖** —— Zakk 是空間背景,圖面才是他的母語
- 圖面在**轉檔時就把白底去掉**(inkify:純黑 + alpha = 255−亮度,等同 multiply),
  所以圖面沒有白框、直接躺在紙色上,背景碎片也透得過來
- **圖面不加圖說**(圖裡本來就印著「東向立面圖 S 1/100」),**渲染圖才加**
- 進場也分兩種:圖面「由左往右描出來」(像繪圖機),渲染圖用全站的 blur→對焦

**④ 檔案**
- `css/work.css`(新)、`works/vegic/index.html`(新)、`.claude/launch.json`(新)
- 內頁**沿用 `js/main.js`**(各段 IIFE 都有防呆,缺元素會自己跳過);
  不放 preloader(使用者從主頁點進來,已經等過一次)

**⚠️ 踩過的坑(重要)**
- **`clip-path` 不能下在被 IntersectionObserver 觀察的元素上**。裁到 100% 時
  可見面積是 0,觀察器判定「沒看到」→ 永遠不加 `is-visible` → 圖永遠不出現,
  **自己把自己鎖死**。改成觀察外層 `figure`,裁切只作用在裡面的 `img`
- **有損的 WebP 透明通道會把線稿壓爛**(alpha_quality 70 就已經一塊一塊),
  去背的圖面一定要 `lossless=True`
- **灰階渲染圖會被誤判成線稿**,去背後變成一坨灰影。分類用兩道保險:
  保守的像素門檻(白>62% 且 彩度<0.02 且 中間調<20%)+ 原檔標明「建模渲染圖」
  的頁碼一律排除;剩下的個案寫在 `FORCE_KIND`

**實測**:19 張圖全載入、28 個進場元素全部觸發、無 console 錯誤、
桌機與 390px 手機都無橫向捲動。

**🔜 下一步**
1. **Enscape 浮水印**:VEGIC 的 10 張渲染圖右下角有試用版浮水印(其他專案都乾淨)。
   等 Zakk 決定怎麼處理(見下方待辦)
2. 渲染圖圖說要 Zakk 校正(自動抓的會把同一區的多張套同一個名字)
3. `facts` 缺「Zakk 在團隊裡負責哪一塊」——**不能編,要 Zakk 給**
4. 模板確認後,寫產生器把其餘 8 件一次做出來

### 1. 素材整理
- 網站素材 **76.2 MB → 2.84 MB**(全部轉 WebP,長邊 2000px)
- 原始檔在 `_source/`,已加進 `.gitignore`,**不進版控**
- 原始 JPG 備份在 `_source/photos-original/`

```
assets/
├─ photos/    12 張  精選景觀(02 Photos 用)
├─ about/      2 張  人像(03 About 用)
├─ 3d/         1 張  dragon
└─ projects/
    ├─ sketches/  5 張  手繪稿(**尚未去背,見下方第 5 點**)
    └─ works/     7 張  從作品集 PDF 抽出的專案圖
```

### 2. Hero + 全站骨架
`index.html` / `css/style.css` / `js/main.js`

### 3. 01 Works + P05
文字清單 + hover 跟隨預覽圖,手機改為列下方直接顯示縮圖。

### 4. 參考 Daima 加的兩個效果(2026-07-23)
分析 `wearedaima.framer.website` 之後挑了兩招:blur 進場、導覽 rolling text。
細節與「沒採用什麼、為什麼」見文末。

### 5. 素描去背(2026-07-23)
工具:`_dev/decrop.py`。成品在 `_dev/clean-*.webp`。
**`assets/` 的原檔還沒覆蓋** —— 等整站完成、所有圖確認無誤再一次換掉。

### 10. Hero B 補狀態列 + About 換夜拍照 + 素描去背版上場(2026-07-23)
- Hero B 底部加回 A 版同款狀態列(Open to work / Based in Taiwan / ↓),主頁不再懸空
- `_dev/clean-*.webp` 三張已覆蓋進 `assets/projects/sketches/`(原稿在 `_source` 有備份)
- 素描縮圖 src 加了 `?v=2` 破快取(Zakk 一直看到舊圖是快取問題,不是檔案沒換)

### 12. 方向修訂「編輯底+動態層」+ 主頁動起來 + About 山頂照(2026-07-23)
- **DESIGN.md 修憲**:看了 to-portfolio / iancoad / roshan-sahu 之後,從「全站安靜」改為
  「編輯底 + 動態層」;紅色 --ember 升格為動態層簽名色。三階段:A 主頁 → B Photos → C preloader
- 結構調整:**About 移到第二區**(01 Works / 02 About / 03 Photos / 04 Contact),導覽同步
- 主頁:副標去 &、拿掉照片落款;**龍骨鬼影背景**(blur 13px + multiply + opacity .15)、
  **大標遮罩升起**(與舊 A 版同語言)、**滑鼠視差**(--mx/--my 由 JS 寫入,CSS calc 位移)
- **About 照片定案:紐西蘭山頂生活照**(IMG_4446,自然帶色 COLOR 0.55,參數在 process_about.py)
- 生活照候選另有:湖邊拿相機 IMG_4766、海邊紅帽背影 IMG_5033(都在 _source/unused)

### 13. A 階段(主頁玩起來)完成(2026-07-23)
- hover 大標 → 龍骨鬼影顯影(opacity .15→.3、blur 13→4px、去灰階上色),JS 加 `.ghost-lit`
- 頂部 ember 紅細線(`.hero-b::before` 漸層)+ 左下暖光(`.hero-b::after` 徑向漸層)
- 照片 hover → 色差 glitch 加強(chroma opacity .4→.62、位移 ±2→±5px)
- 全部走 CSS transition + JS 只切 class;`prefers-reduced-motion` 已涵蓋
- **A 階段收尾了**;接下來 B(Photos 改 iancoad 式)或 C(preloader)

### 27. 修接縫 + 3D 區塊過場 + 載入軌跡(2026-07-26,**目前狀態**)

**① 接縫(Zakk 圈出來的那條線)**
- 原因:氛圍漸層只做在首屏(`.hero-b::after`),**首屏結束處就會跟下面的白底出現一條界線**
- 解法:改成 **`.page-atmos` 固定滿版層**(整頁連續、捲到哪都同一片色),首屏不再自己畫。
  各 section 本來就沒有底色,所以整頁色調一致 —— 這也是「其他頁沒有漸層」的原因

**② 區塊之間的 3D**
- 每區碎片雲加 **`z0` 起始深度 + 透視投影**(`sc = 900/(900-z)`),
  聚攏時從遠/近飛向 z=0;落定後仍留前後擺盪 → 整團一直是立體的
- 近的更大更實、遠的更小更淡

**③ 載入動畫**
- **軌跡**:載入中不用 `clearRect`,改蓋一層 `rgba(paper,0.16)` → 碎片留下殘影,
  看得出是「飛過去組成龍骨」
- **放慢**:進度 lerp 0.07 → 0.028
- **數字縮小很多**:clamp(72–200px) → clamp(26–46px)

**④ 其他**
- About 人像再提亮:brightness 1.42 → **1.75**
- Works **合併重複項**(11 → 9):Kaxabu Map Illustration 併入 Kaxabu Tribe Identity;
  Sugar Reduction Tools 併入 Commercial Space / Sugar Reduction

**🔜 還沒做:作品內頁**
- Zakk 要「照之前作品集的方式大概排」。素材:`_source/portfolio/extracted/` 有 **134 張**
  從 PDF 抽出的圖,依頁碼命名(009–042)
- **卡在「哪幾頁屬於哪個專案」的對照**,要讀 PDF 才能確定 → 下次接手先做這件事

### 26. 全站碎片化 + 骨架裝飾 + Works 補齊(2026-07-26)

**① 分界感 / 導覽**
- topbar 改「上實下透明」的漸層 + 拿掉 border → **沒有硬邊,不會像另一個分頁區**,
  但字仍讀得到、內容捲上來不會直接相撞
- 導覽底線**平常就在**(--hair),hover / 目前位置才轉 ember 橘
  (上一版做成 hover 才出現,字跟背景融合時看不出可按 —— Zakk 指出後改回)
- `Portfolio` 再小再細(clamp 1.25–2.1rem、wght 380、wdth 110)

**② 每一區都有碎片(重點)**
- ambient 模組新增 **`clouds`**:每一區第一次進入視野時生成自己的碎片雲,
  先散在外圈環繞,再**像載入動畫那樣聚攏**到該區周圍,之後緩慢漂浮
- 基礎碎片加量(220→420)、飄動幅度加大、捲動拖曳係數 1.6→3.4(浮誇一點)

**③ 骨架裝飾(Contact)**
- 內嵌 SVG:**棘椎(椎體+棘突)+ 肋骨**,呼應主頁的龍頭骨(主頁維持頭骨)
- 進入視野時**逐根描線畫出來**(stroke-dasharray + 錯開延遲),中段一根肋骨轉 ember 橘
- ⚠️ **坑**:`.bones` 是絕對定位,`#contact` 沒 `position: relative` 會跑去對齊 `#main`

**④ Works 補齊**(原本只有 5 項 → 現在 11 項)
Works Collection / Kaxabu Tribe Identity / Commercial Space · Sugar Reduction /
Kaxabu Map Illustration / Residential Interior / The Little Mermaid /
Wooden Stool / Sugar Reduction Tools / Dragon Skull / Sketches & Forms / Terra Studio
- 預覽圖 lerp 0.15→**0.38**,黏緊游標(之前拖太遠)

**⑤ About / Photos**
- About 人像提亮 `brightness(1.42)`;小節標題改 **ember 橘 + 尾隨橫線**,不再被內文吃掉
- Photos 加說明(拍照是保持觀察的方式/記錄生活)+ 提示;自動捲 72s→**150s**(放慢);
  **滑鼠在照片帶上可用滾輪左右捲**(`--mq-x`,循環不會捲到空的)

⚠️ **踩過的 bug**:preloader `finish()` 後仍有一幀 rAF 排在佇列,會用舊的 `shown`
把 `window.__loadK` 覆寫回去 → **頭骨永遠聚不攏**(實測卡在 0.35)。
修法:`finish()` 內先補 `__loadK = 1`,且 `frame()` 開頭 `if (finished) return`。

實測:**61 FPS**、無 JS 錯誤。

### 25. 載入無縫化 + 側邊區塊列 + 捲動過場(2026-07-26)

- **載入動畫不再分兩階段**:刪掉 preloader 自己的 canvas 與簾幕過場。
  preloader 只剩數字+橘線的**透明浮層**,把成形程度寫進 `window.__loadK`(smoothstep),
  由**首屏那張 canvas** 直接把碎片從外圍環繞插值成頭骨 → 載入本身就是主頁在成形。
  載入中先藏住 hero 文字與 topbar,完成才淡入。
- **右中改成「所有區塊」的線條列**(`.side-nav`,固定定位):一區一條短線,
  滑過伸長並顯示名稱,**目前所在那條轉 ember 橘**(取最接近視窗中央的區塊)。
  取代原本只到下一頁的 next-cue。
- **導覽底線改成 hover 才展開**(scaleX 0→1)。一直都在的話四條線會連成一條橫帶,
  看起來像有交界 —— 這是 Zakk 指出的問題。
- **捲動過場**:捲離首屏時碎片**往下 streaming(位移)+ 往外散開 + 淡出**
  (`sp01 = scrollY / (H*0.85)` 驅動),接到下一區。
- 碎片**再調小**(頭骨 sz 係數 0.36→0.28、繪製倍率 2.4→1.7;ambient 0.9~3.5→0.7~2.5)
- 顏色/位置:`Portfolio` 改 `--stone`(同左下小字);`From space to screen.` 與
  `Open to work` 同為 `--ink`;狀態列移到**頁面中下**(absolute + translateX)

⚠️ **量測陷阱**:預覽窗格在背景時 `document.hidden = true`,rAF 被節流到 1fps、
setTimeout 也只跑 ~9 次/秒。**量到個位數 fps 先確認 `document.hidden`**,
實際可見時是 61fps。

### 24. 首屏改版:碎片頭骨當主角(2026-07-26)

**主風格定調:碎片粒子拼湊骨頭**

**① 首屏重組**
- **人像移除**,改放到 About(About 改用 `hero-portrait.webp` 暗調棚拍;山頂生活照退下)
- **頭骨當主旨**:整片背景就是碎片點雲,**z-index 0 墊在文字後**(之前蓋到大標)
- 「**Portfolio**」中小字(clamp 1.75–3.2rem),放在原大標位置(左、垂直置中)
- 所有說明文字縮到 10.5px mono,**收在左下角**(From space… / designer… / tags / Open to work)
- 拿掉頂部橫條(`.hero-b::before` 與 topbar border);拿掉捲動進度線
- 左上 ZAKK 放大到 15px

**② 粒子改不規則碎片**(不再是方形像素)
- 預先產生 7 種隨機多邊形 sprite(`window.__fragSprites`,依顏色快取),每幀只 drawImage
- ⚠️ **效能教訓**:drawImage 比 fillRect 貴很多,密度沒調就從 61→20fps。
  頭骨取樣 step 由 `iw/300` 放寬到 `iw/200` 後回到 **61fps**

**③ 載入動畫:碎片環繞 → 聚成頭骨**
- 碎片先在外圈環繞漂流,隨真實進度以 smoothstep 插值**聚攏成頭骨**,再交棒給首屏
- 進度仍綁真實載入(字型+非 lazy 圖+load),6 秒硬性收尾

**④ 捲動動態**
- 全站碎片會**順著捲動方向被帶走**(平滑過的 scroll velocity),鬆手回穩
- 每一區都有碎片漂浮(ambient 鋪滿全頁)

**⑤ 導覽與換頁**
- 導覽項下方加線(hover / 目前位置轉 ember 橘)
- **右側中間「到下一頁」線條**(參考 jcedrik):JS 自動注入 hero→works→about→photos→contact,
  hover 時橘線由左掃到右

**⑥ Photos 改橫幅無限滾動**
- `.marquee` + JS 複製一份軌道 → `translateX(-50%)` 無縫循環;hover 暫停細看
- ⚠️ **踩過的坑**:滿版出血用 `100vw` 會多算捲軸寬 → 整頁能左右晃。
  改用 JS 量 `document.documentElement.clientWidth` 寫進 `--vw`;
  **且載入畫面鎖捲動時量到的值是錯的**(沒捲軸),load 後要補量

實測:**61 FPS**、無 JS 錯誤、無橫向捲動。

**dead code 待清**(不影響運作):`.photos`/`.ph--*`、`.hb2-text`/`.hb2-title`/`.hb2-lede`/
`.hb2-tags`/`.hb2-fig`/`.hb2-portrait`/`.hb2-chroma`、`.lgm-*`、`.scroll-progress`、`?debug` 面板

### 23. 載入動畫 + 全站環繞粒子 + 捲動動態 + Photos 錯位(2026-07-26,首屏部分已被 24 取代)
Zakk 授權自行設計,參考 iancoad / roshan-sahu「不制式、多動態」。

**① 載入動畫(preloader)** — DECISIONS §四 排的收尾項,提前做了
- 大數字(tabular-nums 不晃)+ 細橘線 + LOADING;完成後整片**簾幕上掀**
- **綁真實載入**:字型 ready + 非 lazy 圖片 + window load,每完成一項往上推;
  顯示數字用 lerp 追真實進度 → **不是固定時間的假動畫**
- `html.is-loading` 鎖捲動;**6 秒硬性收尾**,任何情況都不把使用者鎖住
- `prefers-reduced-motion` 時整個跳過

**② 全站環繞粒子(.ambient)** — 固定鋪滿視窗、z-index 0、墊在所有內容後
- 微塵緩慢漂移 + 滑鼠視差 + **捲動時比頁面慢**(景深),用 modulo 無限循環
- stone/slate/淡藍灰 + 8% ember;刻意很淡(氛圍不是主角)

**③ 捲動動態**
- 頂部 **ember 捲動進度線**(.scroll-progress)
- 區塊標題 **clip-path 遮罩上掀**(接現有 .reveal 的 is-visible)
- 照片**捲動視差**(JS 只寫 `--py` 變數,交錯正負 → 深淺不同)
- 章節編號改 ember 橘(解「整體太冷淡」)

**④ Photos 錯位排版**
- row-gap 收到 --s6;偶數/3n 交錯下沉(--s5/--s3),幅度克制不留空洞
- 寬度不規則:full 內縮 2/-2、tall 2/span 4、wide span 6 靠下、half 左右對排
- **編號用 CSS counter**(純裝飾,不動 HTML)
- 圖片進場:scale 1.06 → 1 收斂;hover 提高飽和
- ⚠️ **踩過的坑**:半寬照片的欄位起點寫反(odd 給 col 8)會被擠到下一列、
  中間留下大空洞。**每組的「第一張」是偶數,要起在 col 1**,第二張才 col 8

**⑤ 互動溫度**(續解「太冷淡」)
- Works:hover 那列**微微右移** + 編號轉 ember 橘
- Contact:hover 時**橘線由左往右展開**
- 首屏 ↓ 箭頭:輕輕呼吸(cueBob)
- About 人像也吃捲動視差(JS 選擇器加了 `.about__portrait`)

實測:兩套粒子同時跑 **61 FPS**;無 JS 錯誤;各效果都以 computed style 驗證過。

**還沒做(下次可接)**
- 12 張照片的**地點圖說**(等 Zakk 提供)、Instagram 帳號
- Works 02 Terra Studio 仍無預覽圖
- dead code 待清:`.lgm-*`、`.hb2-ghost`、`?debug` 面板、`_dev/`
- 若要更接近 aboutluca 的「3D 穿越」,需 WebGL 重寫(大工程,未動)

### 22. 還原:回到「龍骨點雲 + 人像照片」(2026-07-26)
- Tripo 那隻龍(dragon-full)**不用了** —— 拉長後形狀會跑掉、淺底無發光又太淡
- 還原成第 19 項的樣子:
  - HTML:hero-b 內放回 `.hb2-particles` canvas + `.hb2-fig` 人像;移除全頁 `.page-particles`
  - CSS:hero-b 回雙欄(1.05fr 0.95fr)+ overflow hidden;`.hb2-particles` z-index 2(蓋過文字);
    移除 `.hero-backdrop` 深色首屏與亮色文字那整組
  - JS:引擎換回「Hero 龍骨 3D 點雲」(取樣 `assets/3d/dragon.webp` + 420 顆環繞粒子)
- 已確認無殘留參照(hero-backdrop / page-particles / dragon-full 皆為 0)
- 未刪除的素材(留著備用):`assets/3d/dragon-full.webp`、`assets/3d/螢幕擷取畫面….png`、
  `_dev/process_dragon.py` —— 要清理再問 Zakk

### 21. 深色首屏 + 發光粒子龍(用 Tripo 生的 3D 龍)(2026-07-26,已還原,見 22)
- Zakk 用 Tripo 把參考龍生成**自己的 3D 龍模型**,截圖存 `assets/3d/螢幕擷取畫面….png`
- `_dev/process_dragon.py`:摳深灰底 → 透明底、保留明暗、裁切 → `assets/3d/dragon-full.webp`(539×846)
- 引擎全部重寫(main.js):**取樣整條龍** → 映射到頁面(置中偏右、垂直拉長 2.1x)
  - **柔邊圓 sprite**(radialGradient,無像素感);滑鼠視差;沿身微飄
  - **深色首屏**(`.hero-backdrop` 深青漸層,底部淡出紙白):粒子用 `globalCompositeOperation='lighter'` **加色發光**;龍身進入淺色內容 → 一般混合、漸淡
  - 分 glowGroup(wy<heroBottom)/ normalGroup 兩組,各自 composite
  - 背景星塵(dust)只在深色首屏發光
- 深色首屏配套:hero 文字改亮色、導覽列 `.on-dark` 捲動切換亮/深字(main.js 小 IIFE)
- 效能:drawImage+加色較重,DPR 壓到 1.2、密度 step=iw/135,預覽窗格 **44–46fps**
  (瓶頸是 drawImage 數量;真實 GPU 瀏覽器更順)
- 版權:只用 Zakk 自己 Tripo 生成的龍,**不複製 nightcrow 原畫**
- 待調:發光只在首屏(因選「首屏變深色」);龍身垂直拉長比例、密度、往下漸淡範圍

### 20. 全頁粒子龍(貫穿整頁)(2026-07-23,已被 21 深色發光版取代)
- 參考 nightcrow 青綠龍(**只借氛圍不複製**)+ aboutluca.com(粒子流動過渡)
- 架構大改:粒子 canvas 從 hero 內、改成 **`.page-particles` 固定滿版、z-index 0**,
  內容(topbar/hero-b/main/footer)提到 z-index 上面 → 粒子在所有文字後面
- **主頁人像已移除**(hb2-fig),hero-b 改單欄;龍頭可接龍身
- 引擎(main.js 尾段 IIFE 全部重寫):一條 S 形曲線 splineX(u) 貫穿整頁高度(docH),
  ~10000 顆粒子沿曲線分布(法線方向給厚度 thickAt)、沿身緩慢流動;
  用 scrollY 位移繪製 → 龍身錨在整頁、隨捲動延伸。teal 冷色階 + ember 橘 + 橘色星座線/節點
- 效能:分桶批次 fillRect,10440 rects 實測 **2.1ms/幀**(足夠 60fps)。
  ⚠️ 預覽窗格背景時 visibilityState=hidden → rAF 被節流到 1fps,**那不是效能問題**,要在前景瀏覽器看
- **龍頭找回來了(2026-07-23)**:取樣龍骨雕塑的 3D 點雲(buildHead,明暗當立體、透視、
  滑鼠轉動 + 緩慢自轉),teal 上色,錨在頁首(headAnchor = 龍身 spline 起點),隨捲動移出畫面。
  龍身 spline 從龍頭位置(headY0)起,不再從頁頂 → 頭身相接
- 版權界線:只用 Zakk 自己的 dragon.webp 雕塑,**不複製 nightcrow 那張畫**,只借氛圍
- 下一步可調:龍頭大小/位置、密度、青綠深淺、龍身粗細/蜿蜒、捲動過渡往 aboutluca 靠
- ⚠️ 待清 dead CSS/HTML:.hb2-particles / .hb2-fig / .hb2-portrait / .hb2-chroma / .lgm 系列

### 19. 龍骨點雲:蓋過文字 + 放大 + 環繞粒子(2026-07-23,已被 20 全頁版取代)
- 方向翻轉:不再避開文字。canvas `z-index: 2` **疊到文字與人像上方**,碎片蓋過去
- 龍骨放大(寬吃 110%,超出畫面邊緣);**取樣密度大幅提高**(step = iw/300),
  且碎片大小跟著間距走(`sz = gap * 0.36~0.7`)—— 放大後才不會散成看不出形
- 新增 **420 顆環繞粒子**:分布在比骨頭更大的球殼上(壓扁成環),
  跟同一組 3D 旋轉,所以會繞著龍骨轉、飄過標題與照片
- 效能:改成**依「顏色+透明度」分桶**,每桶只設一次 fillStyle 批次畫。
  上萬顆碎片實測 **61 FPS**(逐顆設 fillStyle 會嚴重掉幀)
- 已移除舊的避讓(measure/clearance)相關程式碼

### 18. 龍骨 3D 點雲升級 + 與版面互動(2026-07-23,避讓做法已被 19 取代)
- 3D 點雲:明暗當 z 起伏、透視投影(F=900)、隨滑鼠轉動 + 緩慢自轉、近大遠小
- 密度提高、放大到近滿版(寬吃 92%)
- **碎片與文字/人像互動**:量出 `.hb2-text` / `.hb2-fig` 的框,靠近就**淡出**
  (羽化 52px)。試過「推開」但會在框邊堆成生硬直線,改用淡出才自然。
  避讓框每 20 幀重量一次,免疫字型載入與動畫造成的位移。
- ⚠️ **重要教訓:瀏覽器快取**。之前改了 JS 卻沒生效(素描舊圖也是同原因)。
  已在 index.html 給 CSS/JS 加 `?v=` 版本號 —— **每次改完 CSS/JS 要把數字 +1**。

### 17. 放棄 logo,改「龍骨碎片漂浮」主頁(2026-07-23)
- 參考 to-portfolio.com:龍骨拆成幾百個小方塊,canvas 動畫漂浮
- 移除透視框 logo(.lgm),大字「From space to screen」還原成主角(兩行、大字級)
- 背景鬼影 img 換成 `<canvas class="hb2-particles">`;引擎在 main.js 最後一段 IIFE
  - 取樣 assets/3d/dragon.webp 的 alpha → 碎片;正弦路徑漂浮 + 滑鼠推開
  - hover 標題(.ghost-lit)→ 碎片聚攏成龍骨;prefers-reduced-motion → 靜態一張
- ⚠️ 待清:.lgm / .lgm-* 和 .hb2-ghost 的 CSS 已無對應元素(dead CSS),收尾時刪

### 16. Logo 改用「設計龍 + ZAKK」(2026-07-23,已放棄,改上面 17)
- 透視框 logo 被否決(太醜);龍骨照片剪影也否決(要設計手法,不要模型)
- 徒手 SVG 畫龍效果不佳(_dev/logo-draft.html);改走 AI 生圖
- **等 Zakk 用 prompt 生一隻設計龍(只生圖案、不含文字)帶回來**,我再:去背/向量化 → 上色成 --ink + --ember → 加 ZAKK(Archivo)→ 進主頁 + 動畫
- 生圖 prompt 已給(monoline / 幾何 / emblem 三版);龍骨煉徽章的工具留在 _dev/process_logo.py

### 15. Logo:一點透視「房間框」+ 當主頁主角(2026-07-23,已被否決,改上面 16)
- 概念:抽象符號「點→框」= From space to screen;A 對稱版定案(探索頁 `_dev/logo-explore.html`)
- SVG 純幾何做在 index.html `.lgm`(外框螢幕 + 內框退向消失點 + ember 紅點)
- 主頁:logo 放大當主角、進場動畫(紅點→透視線→外框,stroke-dash 畫出來)
  `From space to screen` 從大標退成**副標**(font 縮小);lede/tags 延後進場配合節奏
- 互動:消失點深度隨滑鼠視差(.lgm-depth);hover logo/副標 → 龍骨鬼影顯影
- 待辦:要不要把 mini logo 放進導覽列取代 ZAKK 字、以及做成 favicon

### 14. 主頁微調加強(2026-07-23)
- 鬼影平常 opacity .15→.22、blur 減;hover 顯影 .3→.42
- 大標左側 ember 紅刻線(border-left);暖光漸層加到左下+右上兩處
- hover 大標 → 字本身紅/藍 text-shadow 色差抖動(呼應照片 glitch)
- 副標 .hb2-lede / 關鍵字 .hb2-tags 載入往上浮(hbRise,延遲 320/440ms)

### 11. Hero B 定為預設主頁 + 主頁補設計 + About 照片定案(2026-07-23)
- **Hero B 是預設主頁了**,`?hero=a` 才看得到舊純文字版(JS 失效時也顯示 B)
- 主頁新增:身分關鍵字列(mono 小字 PRODUCT · SPATIAL · UX/UI · CREATIVE)+ 照片落款(Zakk Tseng · 曾莛翔)
- **About 照片定案:夜拍半身**(IMG_8992,冷調、保留 30% 原色、底部漸暗)
  參數已寫回 `_dev/process_about.py`;主頁棚拍暖調 vs About 夜拍冷調,不重複
- About img src 加 `?v=2` 破快取

### 8. 02 Photos 區(2026-07-23,排版完成、圖說待補)
- 節奏:全幅 → 兩張並排 → 直幅+橫幅混排,循環;手機單欄
- **圖說(地點)還沒放** —— 等 Zakk 給 12 張的地點,補進 `<figcaption>`
- 整站色彩策略定案:靠照片帶色,介面保持安靜(不加新的介面色)

### 9. Works 01 預覽圖(2026-07-23)
- 用無頭 Edge 截 `cyd2025.framer.website` 首頁 → `assets/projects/works/cyd2025-cover.webp`
- 截圖工具指令記在對話,重截:`msedge --headless --screenshot=... --window-size=1600,1000 <url>`
- Works 現在只剩 02 Terra Studio 沒圖(網站未完成,保留)

### 7. 03 About 頁(2026-07-23,文案照 DECISIONS §三 定稿)
- 版面:冷調人像(左)+ 文字流(右);手機單欄
- 人像:`_dev/process_about.py` 烤冷調版 → `assets/about/about-portrait.webp`(冷藍灰,與 Hero 暖調區分)
- CV:`Downloads\曾莛翔.pdf` 已複製為 `assets/cv/zakk-tseng-cv.pdf`(英文檔名,網址不亂碼)
- ⚠️ about-portrait.webp 約 1MB 偏重,收尾階段要壓小

### 6. Hero B 版:文字+暗調人像(2026-07-23,照片已定案)
- 網址加 `?hero=b` 才顯示;A 版(純文字)完全沒動,JS 失效時預設 A 版
- 版面:原版文字(左)+ 直式暗調人像(右),參考 kalinsky.design
- 人像用 Python 烤製:`_dev/process_portrait.py`
  讀 `_source/photos-original/about/DSC_1729.JPG` → 輸出 `assets/about/hero-portrait.webp`
  裁切定案:九宮格 **2 號標準置中** `(0.30, 0.23, 0.68, 1.00)`,
  暖調 duotone + 顆粒 + 暗角 + 底部漸暗(把牆化進黑裡)
- CSS 端只做:直式框 210×min(48vh,350px) + 輕微 RGB 色差(±2px、opacity 0.4)
- 裁切比較工具:`_dev/crop_grid.py` 產九宮格對照圖(要再選構圖時用它,別一輪一輪猜)
- 相關決策見 `DECISIONS.md`;美感定位(編輯極簡為底、Hero 電影暗調為唯一招牌)見 `DESIGN.md`

---

## 定案的設計決策

### 色票(由 12 張景觀照實際取樣決定)
| 變數 | Hex | 用途 |
|---|---|---|
| `--paper` | `#F4F6F7` | 底色,冷紙白 |
| `--mist` | `#E8ECEE` | 次層 |
| `--ink` | `#171B1E` | 內文墨(中性) |
| `--ink-dsp` | `#16232C` | 顯示墨(偏藍),只給大標題 |
| `--stone` | `#646E75` | 次要文字(對比 4.81:1,2026-07-28 修正,舊值 #6B767D 只有 4.29 沒過 AA) |
| `--slate` | `#5B7386` | 副標、連結、**所有 hover** |
| `--hair` | `#C9D4DA` | 分隔線,**不可拿來寫字** |
| `--ember` | `#B4552A` | 全站唯一暖色,只用在 Open to work 狀態點 |

**SPEC 原本寫「淺色暖白」已作廢** —— 照片實測是冷藏灰,暖白會讓照片發濁。

### 間距尺標
`4 · 8 · 16 · 24 · 40 · 64 · 104 · 168`(`--s1`～`--s8`)
所有垂直間距只能從這組挑。

### 字型
- **Archivo**(可變字型,wdth 62–125)+ **JetBrains Mono**
- **字型檔放在 `assets/fonts/`,不連 Google 伺服器**
  原因:Google Fonts 在這台機器上載不進瀏覽器,導致 Expanded 失效
- 中文目前用系統內建字型。之後要做「台北黑體」子集檔(只含用到的字,約 20KB)

### 動態
| 編號 | 用途 |
|---|---|
| P07 滾動揭示 | 全站入場語言。**含 blur(10px) → 0,600ms** |
| P08 編號章節 | 01–04 閱讀節奏 |
| **P05 hover 換圖** | **主角效果**(Works 區) |
| 導覽 rolling text | hover 微互動,260ms + 每字母錯開 16ms |

- **P01 文字解碼:不做。** 說不出它幫使用者理解了什麼
- **P02 preloader:確定要做,排收尾階段**,綁真實圖片載入進度,不做假進度
- P03 自訂游標:第二版再說

**blur 進場的三個界線:**
- **Hero 大標不加 blur。** 遮罩升起已經在講「還沒成形」,再疊模糊是同一句話說兩次,而且模糊的字邊碰到裁切線會被硬切。分工反而更好:Hero 用「升起」、內文用「對焦」,首屏才不會跟下面長得一樣
- 模糊 600ms、位移與淡入 700ms —— **清晰度比位置早到**,字先看得懂再落定;三個同時結束會有鈍感
- 進場只播一次(播過就 unobserve)。往回捲不重播 —— 已讀過的內容不該被強迫再演一次。**要重看效果就重新整理頁面**

**rolling text 的做法:** 捲上來遞補的那一份**不是 DOM 裡的第二份文字**,是
`text-shadow: 0 1.3em 0 currentColor` 畫出來的複製品,外框 `overflow:hidden` 裁掉。
所以頁面只有一份文字,讀螢幕軟體不會念兩次(連結另外加了 `aria-label`)。
JS 只負責把字母拆成 `<span class="ch">`,用 `--i` 帶順序給 CSS 算延遲。
實測影子位移 14.3px、裁切窗高 14.297px —— 對齊誤差 0.003px。

### 其他
- **Hero 不放圖**,進站第一屏純文字
- 導覽列 sticky(因為章節指示需要它一直看得見)
- **桌機不顯示導覽編號**(文字本身夠清楚,數字是雜訊);**手機才顯示 `01 02 03 04`**,因為四個英文字排不下
- 定位句:`From space to screen.`
- Hero 垂直結構用 Grid 四列分配,標題中心固定落在 40–45%

### Works 清單(2026-07-23 定案)
| | 專案 | 類型 | 年份 | 連結 | 預覽圖 |
|---|---|---|---|---|---|
| 01 | Works Collection | Selected Works | 2025 | cyd2025.framer.website | **無** |
| 02 | Terra Studio | Web / Dev | — | 網站還沒好,先不連 | **無** |
| 03 | Commercial Space / Sugar Reduction | Spatial · Packaging | 2024 | | vegic-store |
| 04 | Kaxabu Tribe Identity | Brand Identity | 2023 | | kaxabu-map |
| 05 | Sketches & Forms | Drawing | — | | sketch-product-thumbnails-01 |

- **沒年份的就留空,不放破折號** —— 七個一模一樣的破折號排一排才是亂的來源
- 原本的「Smart Traffic Signal」和「This Site」已從清單移除

---

## 待辦(下次從這裡開始)

### 🔴 需要 Zakk 提供
0a. ~~VEGIC 渲染圖的 Enscape 浮水印~~ **已決定:就這樣放**(2026-07-28)。
    學生專案用試用版渲染很常見。⚠️ 不要試圖裁掉或修掉 —— Enscape 試用授權
    明文禁止移除/遮蔽浮水印,那是授權問題不是美感問題
0b. **Zakk 在 VEGIC 團隊裡負責哪一塊**(內頁的 facts 缺這一列,不能編)
0c. **渲染圖圖說校正**(自動抓的會把同一區的多張套同一個名字)
1. **12 張景觀照的地點**(圖說只寫地點不寫年份;排版已完成,等地點補 figcaption)
   目前檔名還是 `sdim2377.webp` 這種,拿到地點後要一次改成有意義的名字
2. **Instagram 帳號**(現在連到 instagram.com)
3. **Contact 的 email 要不要換**(現在是 zakk0000g5@gmail.com)
4. ~~Works 01 沒預覽圖~~ **已解決**(截了 cyd2025);02 Terra Studio 仍空,等網站完成

### 🟡 待確認
5. 專案名稱英文 / 中英並置(「噶哈巫」→「Kaxabu」會掉語意)
6. 要不要加燈具、家具、住宅、龍頭骨等其他專案
7. **橘色狀態點**:慣例上橘=離開、綠=有空,要不要換回綠

### 🟢 接下來要做
7b. **Hero B 收尾**:進場動畫、手機版檢查、glitch 要不要動態(~~A/B 抉擇~~ **已定:B 為預設**;確定不回頭後可刪 A 版 HTML/CSS)
8. **02 Photos 區**(12 張,等地點資料)
9. 03 About / 04 Contact
10. `/works/<slug>` 專案獨立頁
11. P02 preloader(收尾階段)—— 版面抄 Daima:大數字 + 細橘線 + 小字 loading,
    但**百分比綁真實圖片載入**
12. 中文字型子集檔
13. ~~把 `_dev/clean-*.webp` 覆蓋回 assets~~ **已完成**(2026-07-23)
14. **刪掉 `js/main.js` 最後的 `?debug` 診斷面板**
15. **刪掉 `_dev/` 資料夾**(去背成品已上場,但烤圖工具 process_portrait.py / process_about.py / crop_grid.py 還在裡面,刪之前確認不再調照片)
16. `git init` → GitHub Pages

---

## 素描去背的細節

**工具:`_dev/decrop.py`**
```bash
python _dev/decrop.py <來源> <輸出> [left|right]   # 第三個參數是線圈在哪一側
```

| 檔案 | 結果 | 備註 |
|---|---|---|
| product-thumbnails-01 | 1349×1887 | 線圈在右 |
| interior-perspective | 1272×1942 | 線圈在右 |
| landscape-perspective | **1890×1214** | 線圈在右,**已逆時針轉正 90°** |
| product-thumbnails-02 | 263×385 | **不採用**,原圖只有 302px 寬會糊 |
| fox-illustration | — | **跳過**,本來就是乾淨掃描稿,沒東西可去 |

**landscape 為什麼要轉:** 是把本子橫過來畫的風景構圖,不是拍歪。
轉正後山稜在上、樹和地面在下、建築站著、天上有鳥 —— 重力方向對了。不轉的話建築是躺著的。

**做法:** 重度模糊(半徑隨圖寬縮放)讓鉛筆線條消失,只剩「紙很亮 / 背景偏暗」,
由外往內找左、上、下三個邊,四條線相交得到四個角,再做透視校正拉成正矩形。
**只動幾何,不動明暗對比** —— 紙的顆粒質感完整保留。

**兩個踩過的坑,別再踩:**
- **線圈在哪一側不能自動判斷。** 木紋和素描的密集筆觸都會產生類似線圈的黑白條紋,
  試了三種方法都會誤判。四張圖不值得為了全自動再繞 —— 由人看圖指定就好。
- **右緣不能「從最右邊往左找第一段乾淨區」。** 線圈外側還有一截本子封面也是乾淨的,
  會讓右緣停在線圈**外面**,線圈就留在圖裡。要先定位線圈本體,再取它的左界。

---

## 重要發現

**作品集 PDF 裡有 SPEC 沒列到的專案**,而且完整度很高:

| 專案 | 類型 | 素材 |
|---|---|---|
| **噶哈巫(KAXABU)牛眠部落品牌識別** | Branding | logo、地圖插畫、海報、名片、摺頁**全套** |
| **VEGiC 商業空間** | Spatial | 十多張渲染 + 完整圖面 |
| 粉紅建築 + 咖啡廳 | Spatial | 渲染 + 圖面 |
| 白色住宅 + 室內 | Spatial | 渲染 |
| 木質椅凳 | Furniture | 3 張 |
| The Little Mermaid 燈具 + 海報 | Lighting | 2 張 |
| 龍頭骨、鐵線雕塑 | Form / Craft | 5 張 |

**噶哈巫是其中最強的一件** —— 有完整識別系統,而且是有文化脈絡的題目,不是習作。

---

## Daima 網站分析(2026-07-23)

參考對象:`wearedaima.framer.website`(Framer 模板)

**採用的兩招:**
- **blur 進場** —— 它的初始狀態是 `opacity:0.001 / blur(10px) / translateY(10px)`。
  多了一層失焦→對焦,文字像「浮上來對準」而不是「飄進來」
- **rolling text hover** —— 做法見上方「動態」段

**沒採用的,以及理由:**
| 效果 | 不做的理由 |
|---|---|
| Lenis 平滑滾動 | 要多載套件,又蓋掉原生滾動,本站不長,收益低 |
| 三層 parallax 卡片 | Works 已經有 P05 當主角,再加會打架 |
| 跑馬燈循環 | agency 拿來充版面的,我們的內容是真的,不需要 |
| 自訂游標 | 與原本 P03 的判斷一致,第二版再說 |

**它的 preloader 是反面教材:** 實測頁面開啟約 1 秒,數字已衝到 099,
但那時 28 張圖只載完 5 張 —— 百分比是固定時間動畫,跟真實載入無關。
**版面可以抄,邏輯不要抄。**

**順帶驗證:** 它的 hover 微互動是 `transform 0.18s cubic-bezier(0.2,0,0,1)`,
和本站 `--t-fast: 180ms` 一致。

---

## 已結案:Archivo Expanded

**確認生效,不需要再處理。** 2026-07-22 在瀏覽器實測(Opera 133 / Chromium):

```
font-stretch             : 125%
font-variation-settings  : "wdth" 125, "wght" 500
wdth 100 → 533.4 px  /  wdth 125 → 687.2 px  /  差距 28.8%
Archivo 已載入 YES ・ 套用中 YES ・ wdth 軸有作用 YES
```

實測 28.8% 與從字型檔算出的 28.9% 吻合。**wdth 125 已是 Archivo 的最寬檔位。**

之前判斷「沒生效」的原因:單獨看沒有比較基準,加上 `letter-spacing: -0.03em`
在 176px 下把字距收掉 5.3px,削弱了變寬的視覺效果。
對照頁 `_dev/hero-compare.html` 有四種字距版本,**Zakk 選擇維持現狀(B)不改。**
