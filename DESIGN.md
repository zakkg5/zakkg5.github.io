# DESIGN.md — 作品集最高設計原則

> 這份是全站視覺的「憲法」。做任何一區之前,先對齊這份。
> 進度看 `PROGRESS.md`、今日決策看 `DECISIONS.md`、互動語彙看 `motion-patterns.md`。
> 最後更新:2026-07-23

---

## 一、美感定位(最重要,先讀)

> **2026-07-23 修訂**:Zakk 看了 to-portfolio.com / iancoad.com / roshan-sahu.com 後,
> 方向從「全站安靜」改為「**編輯底 + 有膽子的動態層**」。舊版「大膽只花在一個地方」作廢。

**底不變:編輯極簡。** 色票、間距、字型系統照舊;內文與資訊區依然安靜、可讀。

**上面疊一層「玩」:**
- **主頁用照片玩**(roshan 式):龍骨圖模糊放大當背景鬼影、大字遮罩動畫、滑鼠視差、hover 特效
- **Photos 區照片就是介面**(iancoad 式):黑底帶狀、不制式混排、metadata 隨捲動更新 —— 整站有亮→暗→亮的節奏
- **preloader**(to-portfolio / roshan 式):數字 + 紅線,綁真實載入
- **紅色升格**:`--ember` 從「只有狀態點」升為**動態層的簽名色**(細線、漸層、hover 閃現);內文區仍不用

**還是有界線(玩不等於亂):**
- 動態都要**服務內容**(引導視線、給回饋),純炫技的不做
- 內文、經歷、清單這些「閱讀區」不加動態干擾
- `prefers-reduced-motion` 時全部安靜下來,內容照常

**服務對象:** 設計為主,但非設計職看了也不排斥(見 `DECISIONS.md` §一)。

**招牌符號(2026-07-23 新增):** 一點透視「房間框」logo —— 螢幕比例外框 + 退向消失點的內框 + ember 紅消失點。同時是「3D 空間」與「平面螢幕」,即 From space to screen 的畫面。主頁當主角(進場:紅點→透視線→外框),`From space to screen` 退為副標。SVG 幾何在 index.html `.lgm`,探索頁 `_dev/logo-explore.html`。

---

## 二、色票(由 12 張景觀照實際取樣決定)

| 變數 | Hex | 用途 |
|---|---|---|
| `--paper` | `#F4F6F7` | 底色,冷紙白 |
| `--mist` | `#E8ECEE` | 次層 |
| `--ink` | `#171B1E` | 內文墨(中性) |
| `--ink-dsp` | `#16232C` | 顯示墨(偏藍),只給大標題 |
| `--stone` | `#646E75` | 次要文字(對比 **4.81:1**) |
| `--slate` | `#5B7386` | 副標、連結、**所有 hover**(4.57:1) |
| `--hair` | `#C9D4DA` | 分隔線(1.39:1),**不可拿來寫字** |
| `--ember` | `#B4552A` | **全站唯一暖色**(4.53:1),動態層的簽名色 |

> **2026-07-28 修正**:以上對比是用程式對 `--paper` 實測的,不是估的。
> 舊的 `--stone` `#6B767D` 只有 **4.29:1**,沒過 WCAG AA 的 4.5(文件原本誤寫 4.6),
> 而它用在導覽、圖說、章節編號,影響範圍很大。壓暗 3% 改成 `#646E75` 就過了。
> `--slate` 與 `--ember` 都是低空飛過,**不要再把它們調亮**。

**鐵則:**
- 暖白已作廢——照片實測是冷藏灰,暖白會讓照片發濁。
- `--ember` 是唯一暖色,珍惜它;除了狀態點與 preloader 細線,不要亂用。
- Hero 招牌人像的「暖」來自照片處理(暖調單色),不是動色票。

---

## 三、字型

- **Archivo**(可變字型,wdth 62–125)— 標題與介面。大標用最寬檔位 **wdth 125**。
- **JetBrains Mono** — 編號、圖說、小標籤。
- **字型檔自託管在 `assets/fonts/`,不連 Google 伺服器**(這台機器載不進,會讓 Expanded 失效)。
- 中文暫用系統字型;之後做「台北黑體」子集檔。

---

## 四、間距尺標

`4 · 8 · 16 · 24 · 40 · 64 · 104 · 168`(`--s1`～`--s8`)
**所有垂直間距只能從這組挑**,節奏才一致。

---

## 五、動態與招牌原則

**全站入場語言:P07 滾動揭示** — 含 `blur(10px) → 0`,像「浮上來對焦」。
- 清晰度比位置早到(模糊 600ms、位移淡入 700ms),字先看得懂再落定。
- **進場只播一次**(播過就不重播)。要重看效果就重整頁面——這是刻意設計,不是 bug。

**Works 主角:P05 hover 換圖** — 純文字清單滑過時預覽圖延遲跟隨。這是內容區唯一的主效果。

**導覽:rolling text** — hover 時字往上捲,用 `text-shadow` 畫替身(不複製 DOM,無障礙與效能零代價)。

**Hero 招牌:暗調人像 + 輕微 RGB 色差 glitch** — 克制。這是「大膽的那一下」,其他地方都不要 glitch。

**明確不做**(見 `DECISIONS.md` §四):三層 parallax、Lenis 平滑滾動、內容區跑馬燈循環。

**底線(不張揚地做到):** RWD 到手機、鍵盤 focus 看得見、`prefers-reduced-motion` 關動畫時內容照常顯示。

---

## 附:這份怎麼用

- 我(或任何 AI 工具)做設計前先讀這份,確保風格穩定貼近 Zakk 的品味。
- 看到喜歡的外部參考(如 kalinsky.design)先歸檔、抽出「觀念」,**不照抄別人的 token**——照抄會變 templated,正是要避免的。
